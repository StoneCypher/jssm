# jssm Drain Ledger — all 107 open issues, individually dispositioned

> **Status:** stable (Fable first-pass, 2026-07-04; awaiting John's batch approval) ·
> **Universe:** `issue-snapshots-2026-07-04/jssm_open.tsv`. Policy: jssm tracker → EMPTY.
> **Codes:** `MIG-eN` migrate to fsl twin ("was jssm#NNN"), tagged era N · `KEEP(x)` stays
> open briefly, closed by the named PR/workstream · `SUP(x)` close, superseded by x ·
> `DONE?` verify shipped then close · `DUP(x)` duplicate · `ASK` John decides.

922 MIG-e0 — CI heavy-job gating; fold into dragon-lane decision (WP-6 Q3)
921 KEEP(6.0 PR) — dist/deno drop rides the assembly branch
920 MIG-e1 — cm6 editor sketch perf fixes (wc track)
919 MIG-e1 — fence follow-up tail incl. semantic coloring
909 MIG-e1 — state kinds umbrella; needs spec (omissions C)
897 MIG-e1 — fsl-workbench shell (wc)
896 MIG-e1 — editable data inspector (wc)
895 MIG-e1 — in-SVG theme legend (viz)
894 MIG-e1 — fsl-timers panel (wc)
893 MIG-e1 — fsl-diagnostics panel (wc)
874 MIG-e1 — perf backfill c8g + 5.45–5.65 gap (perf track)
871 DUP(870) — identical Devvit issue; close one
870 MIG-e7 — Reddit Devvit app (distribution)
869–831 MIG-e7 — the AI-distribution block, one fsl twin EACH, grouped under an era-7
  distribution umbrella: 869 registry richness, 868 articles/Wikipedia, 867 StackOverflow,
  866 Reddit seed, 865 tree-sitter (twin notes SAT: grammars ship from satellite repo),
  864 agent frameworks, 863 custom GPT/Gem, 862 crawler posture, 861 proof-corpus-as-RAG,
  860 FSL-for-LLMs reference, 859 Context7/DeepWiki, 858 llms.txt, 857 corpus visibility
  umbrella, 856 AGENTS.md (cheap: may just do in era 0), 855–853 agentfile family,
  852–847 skills family, 846 umbrella, 845–831 MCP registry listings
830 MIG-e5 — M6 receipts capstone
829 MIG-e5 — M5 verify verb
828 MIG-e1 — M4 property evaluator (subset era 1, full with P2)
827 KEEP(6.0 PR) — M3 implemented on v6; the One Merge PR closes it
826 MIG-e1 — M2 signing/log (attempts era 1, slips to era 5 — decided)
825 MIG-e1 — M1 check + certificate record
822 MIG-e1 — doc-surface inventory for v6 (bridge to help-bar manifest, omissions D7)
820 MIG-e1 — wc theme via URL param
819 MIG-e1 — web replay shell over M3 engine
818 MIG-e1 — proof-construct grammar pinning (C3 slice; blocks corpus)
817 MIG-e1 — proof-example corpus (user-flagged high value)
815 MIG-e6 — cross-host byte-identical determinism
813 MIG-e4 — durable-execution posture (era-4 anchor issue)
798 MIG-e1 — re-land pick/oracle/graph-highlighter (branches exist)
793 MIG-e1 — wc panel-suite track (umbrella twin)
792 MIG-e1 — formatter/pretty-printer (WP-1.10; merge with fmt-verb twin of 621)
790 ASK — DeBERTa/phantom-models side project; scope unclear
778 MIG-e7 — machines.txt RFC
759 KEEP(fix PR) — val numeric-enum bug (era-0/1 tail)
758 KEEP(fix PR) — validator silent no-op
757 KEEP(fix PR) — val/prop collision
756 MIG-e1 — columnar vals + undo journal (perf keystone; envelope first)
755 KEEP(fix PR) — non-null hole
754 KEEP(6.0 PR) — atom charset restriction ships in the breakage batch
743 MIG-e1 — zero runtime dependencies
720 MIG-e1 — constructor cleanups (perf track)
719 MIG-e1 — timer gating perf
718 MIG-e1 — hook fast-path perf
717 MIG-e1 — parser Subexp frame perf
716 MIG-e1 — ArrowDecoration first-char gate
715 MIG-e1 — NonNegNumber merge
714 MIG-e1 — arrow-token scanner
713 MIG-e1 — expectation-free parsing
712 MIG-e1 — transition_impl inline (biggest lever)
690 MIG-e2 — Observable support (revisit against §14 channels)
686 MIG-e2 — wc SSR
685 MIG-e2 — Angular wrappers
684 MIG-e2 — framework-wrapper generator
666 MIG-e7 — examples recipe gallery component
650 MIG-e1 — extract FSL repo w/ versioned spec + conformance (the contract's housing question)
631 DONE? — CLI config shipped (loader + render integration); verify then close
630 MIG-e1 — repl verb
629 MIG-e1 — lsp verb
628 MIG-e1 — mcp verb (phased after verb surface stabilizes)
627 MIG-e1 — playground verb
626 MIG-e1 — convert verb (partially = shipped import/export; note S3)
625 MIG-e1 — init/scaffold verb
624 MIG-e1 — typegen (ungated, TS-first)
623 MIG-e1 — check verb (= M1; merge with 825's twin)
622 MIG-e3 — test verb / MBT framework
621 MIG-e1 — fmt verb (merge into 792's twin)
620 MIG-e1 — lint verb
619 MIG-e1 — CLI umbrella (twin, cross-linked to era umbrellas)
610 MIG-e7 — package.json machine exposure (pairs with 778)
607 MIG-e1 — theme files + auto-load
