# FSL v-next — Manual Topics & Rationale (notes for a later doc-writing pass)

> **Purpose:** a roadmap for the Claude pass that will write the **user manual**. The authoritative *design* lives in `2026-06-09-fsl-megaspec.md` (read it for the "what"); this file says **what the manual must teach, the decisions/rationale to explain (the "why"), and the gotchas to warn about.** Maintained alongside the megaspec — extend it whenever a feature is added.
> **Issues:** StoneCypher/fsl #1340–#1362 (structural, contracts, wildcard, lifecycle, alphabet, temporal, dense-time, PTA).

---

## Cross-cutting principles to thread through the WHOLE manual
These recur everywhere; explain them once, early, and refer back:

- **Bounded by default, opt into unbounded knowingly.** Finite types, bounded containers/strings, tape retention, the microstep bound — every default is the safe/analyzable one. *Why:* keeps machines verifiable and memory-safe; going unbounded is always a visible act.
- **The `finite`/checkable tier is the verification substrate.** Three bands: small-finite (enumerate), large-finite (SMT), infinite/rich (bounded analysis only). *Why:* finiteness is what makes model-checking decidable. The `finite` attribute *enforces* the line.
- **Determinism & replay: input is the single source of truth.** Seeded RNG + recorded input (events *and* sensor reads) → everything else regenerates. *Why:* reproducible runs, portable repro bundles, counterexamples-as-tapes.
- **Total expressions.** No loops/recursion/effects in expression position; iteration only via bounded HOFs. *Why:* every expression terminates; guards always have a value.
- **Host-agnostic.** Spec defines semantics; per-host runtime; a conformance suite is the executable definition; never borrow host behavior. *Why:* one `.fsl` runs identically on JS/Rust/C/Erlang/….
- **The pinning principle.** Pin anything not uniquely determined by input → identical cross-host output: hash *algorithm*, compression *encoder*, Unicode *version*.
- **Behavioral (source) vs observational (API).** Source *defines* what the machine is (`assign`/`where`/entry/exit/handlers); hooks *observe* what it does. *Why:* keeps `.fsl` files meaningful and verification clean.
- **Units = dimensions + nominal branding.** Dimensional analysis catches `m/s` errors; branding catches `user_id` vs `order_id` (and torque vs energy). You need both.

---

## Organizing principle — Domain playbooks
Structure the manual's second half as **playbooks** — entry by occupation, not by feature: **games** (ghost AI, weapon cycles, behavior trees, the RCT park), **fintech** (payment lifecycle, double-entry invariant, margin calls), **embedded** (thermostat → railway interlocking → `settles_within`), **web** (checkout, feature flags, durable sagas), **infra** (CI/CD, leases, drift detection), **AI agents** (guardrails, the repair loop, MBT). Each playbook: three certified-stdlib parts, one flagship worked example, the domain's killer feature named in its own dialect. People adopt by finding *their* chapter. Also extract the **positioning essay trio** for fsl.tools — "You don't write the patterns, you write states" (GoF), "The automata zoo is a coordinate system" (§27 meta), "AI writes it; the verifier vouches for it" (§25.1, guardrails as centerpiece — keynote material) — these already exist inside the spec; publication is extraction.

## Part 0 — The on-ramp
- **Learn-by-repairing (build the manual's intro track this way).** Each lesson is a deliberately broken machine + its counterexample tape; the reader fixes until `check` passes. Graded ladder: syntax error → wrong guard → missing transition → contract violation → temporal-property violation → composed-system route bug. *Why:* it teaches the repair loop AS the learning loop, works identically for humans and models, generates the bug-fix eval corpus as a byproduct, and every lesson is doc-tested by construction. *Pair each lesson with the certified-stdlib part it ends up rebuilding* (lesson 4 ends with "you just built the circuit breaker — here's the certified one").
- **The certified stdlib as teaching vocabulary.** Introduce debounce / retry-with-backoff / circuit breaker / rate limiter / saga / turnstile early and reuse them as the running parts-box; teach reading an interface card before teaching writing a machine.

## Part I — Foundations
- **What FSL is now.** A state machine that is *also* a typed data machine, a transducer (I/O), and a verifiable/testable model. The `prop` (where you are) / `val` (how you got there) / `sensor` (what's out there) trichotomy. *Gotcha:* this is a major-version leap; set expectations. (megaspec §1)
- **GoF design patterns as *features* (positioning/teaching asset).** ~10 of the 23 Gang-of-Four patterns aren't *implemented* in FSL — they **are** language features: **State** (the machine + function-typed props), **Strategy** (function-typed `val`), **Observer** (hooks), **Memento** (serialize / snapshot / tape), **Command** (typed-payload events + tape), **Visitor** (`case` over ADTs), **Mediator** (`System` + `wire`), **Chain of Responsibility** (transition resolution order), **Composite** (state groups), **Decorator** (hooks-as-decorators). ~6 more are naturally expressible (Iterator/Interpreter/Adapter/Proxy/Builder/Prototype); the creational OO-instantiation ones (Factory Method / Abstract Factory / Singleton) are out of a state machine's domain unless actor-spawn is added. *Teach:* lead with "you don't *write* these patterns, you write states" — State/Strategy/Observer/Memento first. (analysis 2026-06-10)
- **The automata zoo — FSL models many formalisms as feature *configurations* (a positioning chapter, sibling to the GoF one).** From **Systems + wiring:** protocols, **session types**, CFSMs, I/O & interface automata, **workflow/BPMN/DMN**, cellular automata, synchronous languages (Esterel/Lustre). From the **model-class ladder:** regex, CFG-parsers/RSMs, visibly-pushdown/nested-word, Petri, tree automata, attribute grammars. From **weighted edges:** Markov chains/MDPs, weighted automata, HMMs, reward machines. From the **transducer model:** Mealy/Moore, FSTs, tree transducers. From **vals-as-registers:** register/data automata. From the **verifier:** Kripke structures/LTS, Büchi. From **groups+history:** statecharts/UML/SCXML. *Meta to lead with:* these are **coordinates** in a feature space (memory × communicating-copies × weights × I/O-shape × registers) — **recovered, not implemented** (same framing as "GoF patterns are features"). *Name the honest non-fits:* timed automata/PTA (deferred), hybrid automata (continuous ODE — off-model), π-calculus mobility & unbounded channels (the totality ceiling). (megaspec §17 catalog) *Worked example to anchor it:* BPMN's parallel gateway → why you need rung-2 tokens (a plain FSM has one current state; tokens are a multiset). *Two more framings worth teaching:* FSL + contracts is a lightweight **executable / visual TLA+ / Event-B** (state + invariants + next-state); and the **inverse of verification** — reactive/LTL **synthesis**, supervisory control, Angluin **L\*** learning — *generates or infers* a machine from a spec/oracle. *Hot example to lead with:* **behavior trees** (game-AI / robotics = a `tree` ticked synchronously).
- **Migration.** `var`→`val`, `property`→`prop` (deprecated alias, **removed in v7**); the assignment verb is `assign` (because `set` is now the set *type*). *Gotcha:* hand-bumping is forbidden; releases are coupled to `main`.

## Part II — Data & Types
- **`val` (extended state).** Declare, type, `default`, `required`, the `vals` config option; read `.val()`, write via `assign` (slot assignment `a[i]:=`, sequential, `old` pre-state). *Decision:* imperative/sequential assign; `old` is the pre-transition accessor. (§5, §9)
- **`prop` (now typed).** State-bound constants, optional type annotations (backward-compat: unannotated stays legacy). (§5)
- **The numeric tower.** `int` (s53), sized `int8..128`/`uint8..128`, `longint`, `float`/`double`, `decimal` (banker's rounding), bounded forms. *Decisions:* receiving-type promotion; overflow = error + opt-in saturate (no wrap); IEEE NaN + `isnan`/`isinf`/`isfinite`. *Gotcha:* bitwise on **sized** ints only, not bare `int`/`longint`. (§4.1)
- **Strings.** Codepoint default, grapheme via `+` suffix, UTF-8 byte view, negative slicing. *Gotcha:* bounded `string len 0..N` for the finite tier. (§4.1, §8)
- **Containers.** tuple/array/set/map/record (+ bounded `len`/`size`); **no `object`** (use ADTs + `any`). *Decision:* keys/set-members = number/string only; values = anything. (§4.2)
- **ADTs (sum types).** `type Shape = circle(r) | rect(w,h)`; recursive ⇒ rich-tier. *Why:* pattern matching is built for them. (§4.3)
- **Variants.** `option`, `any` (open variant, **sound-narrow-only** via `case`); `null`/`undefined` (JS semantics, distinct, forbidden in arithmetic, declared-nullable `T?`). *Gotcha:* `null = undefined` is false. (§4.3–4.4)
- **Type aliases.** `type Celsius = int -273..1000`. (§4.4)
- **Units (the big one).** Dimensioned types, the SI prelude (base units + prefix system → `km`/`ms`/`μA` free + derived units), declaring units (product *and* quotient), dimensional checking, auto-conversion, domain newtypes. *Decisions:* phantom types / **zero runtime cost**; in-scope (later phase). *Gotchas to teach with examples:* the km/h↔m/s and bits↔bytes wins; **torque ≡ energy** (dimensional blind spot → fix with branding); **affine** temperature (°C/°F vs K); `KB`(1000) vs `KiB`(1024); square dollars = variance, sqrt halves exponents. (§4.5) *(Manual should reuse the worked examples: battery charger, cruise control, download manager, rocket controller, risk monitor.)*

## Part III — The expression language
- **Operators & precedence.** Arithmetic (`mod`/`rem`/`div`), comparison (cross-type = error; total `compare`), the boolean zoo (`and`/`or`/`nand`/`xor`/`implies`/… + short-circuit `andalso`/`orelse`), bitwise, `in`, `++`, `|>`. (§6)
- **Control & matching.** `if/then/else` (expression), `let … in`, `case` (full pattern matching, destructuring, `when`, exhaustiveness + `else`), bounded quantifiers `forall`/`exists`. (§6)
- **Container protocol & lambdas.** `map`/`filter`/`foldl`/`foldr`/`all`/`any`/… ; bracket-index **throws**, `get` returns **option**; uncurried lambdas (inline checkable + first-class rich), by-value read-only capture. *Gotcha:* no `foreach` (use `fold`). (§6)
- **Function-typed props & vals (a flagship teach point).** Because lambdas are first-class values, a `prop` or `val` can *be* a function. A function-typed **prop** = behavior that changes with state = the **State pattern**, native and verifiable (one lambda pinned per state). A function-typed **val** = a reassignable **carried strategy**. *How it stays sane:* function values **defunctionalize** to `(stable tag, by-value captures)` so they serialize / hash / replay / cross-host like any data — there is no opaque host closure (this is *why* the totality + by-value-capture + host-agnostic-semantics constraints exist). *Gotchas:* equality is **intensional** (same code+captures, *not* same behavior — two lambdas computing alike compare unequal); function-typed vals widen the verifier's state space (finite-checkable only if the assignable-lambda set + captures are finite-typed); editing a lambda body invalidates old repro bundles (tag = AST content-hash). (§4.4 / §5 / §15)
- **Math & stdlib.** The builtin catalog; trig in **degrees** (with `deg`/`°`/`rad`), RNG distributions; blessed infra: encoding (`base64`/`base64url`/`hex`/percent), pinned `hash`, compression (`zstd`+`lz4`, URL-safe via base64url). *Principle:* domain-neutral core; domain toolkits = userland. (§7)

## Part IV — Behavior, errors, lifecycle
- **Execution model — macrostep / microstep / stable (THE keystone; teach it first in this Part).** A **macrostep** = one external stimulus run-to-completion; a **microstep** = one fired transition; **stable** = the quiescent config where no transition is enabled. *Why keystone:* invariant checks, val-commit, journal-rollback, post-macrostep hooks, observers, serialization, and the tape **all key off "stable."** *Teach the pipeline:* before-macrostep → fire (exit → `require` → `assign` → `ensure` → entry → raise) → cascade eventless/internal → stable (invariants → commit → at-stable). *Plus:* deterministic microstep order (priority + doc-order; nondeterminism only via seeded `probabilistic`); internal-before-external (SCXML FIFO); microstep bound faults runaway eventless; verifier proves quiescence for `finite`. (§12)
- **Guards & contracts (keep the four apart).** `where` = enabledness (silent non-transition); `require` = caller obligation (misuse = error); `ensure` = machine obligation (with `old`); `invariant` = always-at-stable (RTC). *Violation model:* each is a **proof obligation** the verifier discharges **and** a runtime assert that **compiles out under `sealed`/proven**; contract exprs total/pure; **no inheritance**. (§10, #1355)
- **The error model (teach the three surfaces).** in-machine `last_error` (option<Error>, sticky) + `error_count`; in-language composable `error` **tape**; host-side `rollback` **event** (carries the pre-error snapshot); behavioral **error-recovery transitions** (`require X else -> S`, `on error -> S`). *Decisions:* atomic rollback + notify; **no `on rollback` handler** (breaks atomicity); domain **error states** (`state X: { error; }`, local form of `failed_outputs`) ≠ runtime faults. (§11, §17)
- **Lifecycle & hooks.** Source: `on construct`/`destruct`/`entry`/`exit`, `on rejection`, `on any transition`/`any action`; **named handlers** (internal reusable effect blocks); **named hooks** (caller-provided callbacks the machine `call`s, optionally `required` = dependency contract). *Decision:* behavioral-vs-observational split (25 runtime hooks → only ~4 lifecycle deserve source). *Gotcha:* observational hooks are pure observers; introspection (`has_hook`/`hooks_on`) is currently stubbed. *Lineage:* `hooks: open/closed` (fsl **#617**, grammar #644/compiler #645) = the open/closed hook surface; named hooks + `hooks: closed` *fulfill+extend* it (allowlist → typed `required` contract → codegen hook-interface export). **New from RTC:** macrostep-level hook points `before-macrostep`/`at-stable`/`post-commit` (the run-to-completion boundary points that didn't exist before). (§12, §3)

## Part V — I/O, tapes, systems
- **Reactive I/O.** Input via `on chan(payload)` (no `consume` — input *drives* the machine); output via `emit chan <- v`; Mealy + Moore; named typed channels. (§14)
- **Sensors & time.** Sensors = input-level dual of props (read-only, externally wired, recorded for replay); **time = a pluggable, caller-provided sensor** (default clock, tick source with `.advance`, custom), declared abstract model for verification. *Teach the shared-clock multi-machine simulation.* (§14)
- **Tapes.** Input tape = the single retained source of truth (default retention 100,000, tunable/unlimited); output/log/error tapes **not retained** (regenerate by replay); `log`/`error` standard, separate from main output; tokenizer = composition; first-class tapes. (§14)
- **Multi-machine systems.** `machine {}` blocks + `wire` + `import`; `Machine` (`from`/`sm`) vs `System` (`system_from`/`ms`); the system fns (`make_system`/`compile_system`/`serialize_system`/`deserialize_system`); per-machine API via `.machine(name)`; the web component auto-detects. *Decisions:* two types/no-union; sync shared-clock default; private+shared scoping. (§14)
- **Three composition axes (teach *when to use which*).** **Group hierarchy** (`&group` — one machine, shared tape/clock — Composite), **machine-val embedding** (`val sub : machine T` — by-value, owned, parent-driven, snapshotted-with-parent — HAS-A; the Factory-pattern primitive), **system wiring** (`wire` — peer machines — Mediator). *Rule:* **embed when you own it, wire when they're peers, group when it's one machine's sub-states.** *Gotcha (the why):* embedding is an acyclic **tree** (by-value, no self-containment, rich-tier like recursive ADTs); wiring is a possibly-**cyclic graph** (A↔B handshakes) — that topology difference is exactly why machine-vals **don't subsume** Systems; both still share one `Steppable`/`Serializable`/`Renderable` + columnar/journal/tape engine. (§14)
- **Factories (a flagship chapter — the factory_girl / stochastic-tester story).** A `required` val with no `default` **is** an unfilled parameter; the `factory` block binds vals + identity targets (`instance_name`/`machine_name`/`start_at`/`seed`); `with()` curries; `make()` vs `at(n)` (pure core, cursor sugar). *Teach with the RCT park:* a customer factory whose `name <- "Cust ${seq()}"` and `patience <- rand(20, 95)`. *Decisions to explain:* instance *n* of seed *s* is a **coordinate, not an anecdote** (replayable fixtures); construction-time `rand` keeps machines `finite`-eligible (over-approximation — randomized fixtures, verifiable instances); unseeded = clock-seeds-once-at-root **and is recorded** ("I can't reproduce it" stops being a sentence). *Gotchas:* one factory block per document; bindings read earlier bindings only; host-callback bindings = non-portable/non-serializable; `UnboundParameterError` lists *all* missing names. (§21)
- **The seed tree.** One root seed; everything below derives via `derive(parent, label)`; per-instance, per-call-site streams. *Teach:* parallel population builds with zero coordination; changing one binding never perturbs another's stream. (§21.2)
- **Systems orchestration: populations, routes, lifecycle.** `many Customer via factory count n max 500`; routes (`on customers.left(s) -> gate 'depart'(s)`) vs plain `wire`; observation sugar (`enters`/`exits`/`takes` — machines never know they're in a system); `on action` = the system's inbound surface; `where` selectors; `spawn`/`retire sender`. *Teach the dispatch model:* deterministic FIFO, document/write/creation order, system quiescence — same seed + same stimuli = bit-identical run. *Gotchas:* `sender` is the only context name (no computed member refs); `seq()` indices never reused after `retire`; queue-depth bound is the system-level microstep bound; `on_undeliverable` defaults to **error** (loud) — `dead_letter` is the inspectable opt-in. (§22)
- **Systems in systems.** Member contract = actions in, channels out — systems satisfy it too; child runs to quiescence, emissions bubble to the parent; **containment is acyclic (enforced), wiring may cycle** — teach the difference. Now *four* composition axes with groups/embedding/wiring: **group when it's one machine's sub-states, embed when you own it, wire when they're peers, contain when you orchestrate a fleet.** (§22.4)
- **Overlapping state groups.** `&group : [...]`, nest `&c` / spread `...&c`, **group config objects** (`&g : [members] { config };` or separate `&g : { config };` — applied to all members, group-metadata tier of the cascade), the unified styling cascade, boundary hooks, per-group history. *Teach:* > hierarchical states, *represents* Harel (compact orthogonality = parallel regions, deferred). *Gotcha:* it's `:` not `=`; `[…]`=members vs `{…}`=config; state-specific config beats group; nearer/deeper group wins. (§19)
- **Transitions: multigraph & wildcards.** Default 1 edge; **action names** to get more; ≤1 unlabeled edge (= what `transition(to)` takes); probabilistic edges are the rolled exception; wildcard `*` source + `'*'` action. *Teach the resolution order:* specific-action > wildcard-action > unlabeled > wildcard-source > rejection. **Probabilistic list target** `a P% -> [b c]` — list targets *already* fan out (`ArrowTarget ⊇ LabelList`). **Model:** origin-`%` = the **group's** weight; optional per-member `%` *inside* the list = within-group shares; multiply (`a 50% -> [b 20% c 80%]` → 10%/40%; inner omitted → uniform). *Teach:* syntax-compatible with old `P% -> [list]`; the group keeps its stated weight (vs the old copy behavior, which differed only with sibling edges). Full control via inner weights. *Same weighted-`LabelList` as `start_states` — cross-ref Weighted start states; teach them together as one construct in two positions.* (decision log)

## Part VI — Verification & testing (a flagship part of the manual)
- **The verifiability tiers & `finite`.** What's allowed/forbidden; the capability/**disallow** attributes & profiles (`finite`/`pure`/`sealed`/`deterministic`/`stateless`); each buys docs + checker assumption + runtime specialization. **`finite` vs `checkable` (must spell out explicitly):** `finite` = the hard *enumerable* line (small-finite, exhaustible); `checkable` = the softer *"the checker may attempt it"* tier (large-finite/SMT). They are **different attributes** and the manual must teach the boundary with concrete "this is finite / this is only checkable / this is rich" examples. (§3)
- **The automata ladder & the model-class attribute.** A machine declares its rung — `regular`/`pushdown`/`petri`/`tree` — which sets the decision procedure *and enforces* the decidability tripwire (pushdown: **one** push/pop stack, no second stack; petri: monotone counters, **no zero-test**; tree: finite carrier). *Teach:* the val type *is* the auxiliary memory, the rung *is* the language class, totality caps below Turing. **`tree`** gives **verifiable validators / type-checkers** (recognizers) *and* **tree transducers** = **verifiable, schema-preserving compiler passes** (the basis for transcompilation, #4). *Gotcha worth a callout:* dropping the zero-test / the second stack isn't pedantry — it's the exact line between decidable and Turing. (§3/§17)
- **Petri nets ⇆ VASS (the `petri` / rung-2 model class) — teach both names, and that they're the *same machine*.** A **Petri net** is the token/graphical view (places hold tokens; transitions consume/produce; a transition fires only if its input places have enough tokens). A **VASS** (*Vector Addition System with States*) is the counter/automata view: a finite-state control + **k non-negative integer counters**, where each transition adds a fixed integer **vector** to the counters and is enabled only if they stay **≥ 0**. They're inter-translatable — **places ↔ counters, firing ↔ vector addition, token-availability ↔ non-negativity**. *The point for FSL:* **rung 2 literally *is* a VASS** — "a state machine **+ a `bag` val**" = finite control + counter vector; `bag[x] += 1`/`-= 1` are the vector adds, `>=` guards are the non-negativity constraint. (Petri nets are how you *draw* it; VASS is how it's *built*.) *Decidability to teach:* **coverability** ("can a counter reach/exceed N?") is decidable, EXPSPACE; **reachability** is Ackermann-complete; and the **no-zero-test** rule is load-bearing — a zero-test turns it into a Minsky counter machine → Turing → undecidable, which is why `petri` *forbids* it. *Worked example:* producer/consumer with an unbounded buffer. (§3/§17)
- **The property-pattern catalog (teach BEFORE raw temporal logic).** Dwyer patterns as the front door: response, precedence, absence, universality, existence — each × scope (globally / before / after / between). *Teach:* choose a pattern, fill the holes; raw LTL is the escape hatch, not the entry; `lint --suggest-properties` proposes candidates from structure. *Why:* ~92% of real-world specs fit the catalog, and miswritten `eventually` formulas are the classic verification footgun — for humans and models alike. (§17)
- **Refinement & safe evolution.** `diff --behavioral` (v2 simulates v1) + refinement certificates; teach the upgrade story: prove compatibility, don't test for it. *Gotcha:* refinement is over the *old alphabet* — new behavior on new actions is free. (§25)
- **Temporal properties.** Safety (`always`/invariants) vs liveness (`eventually`/`leads-to`/`until`/`weak-until`/`always eventually`/…), past operators (`once`/`since`), bounded (`within N`), probabilistic (PCTL). *Teach:* counterexamples are **replayable tapes** (finite trace / lasso). Dense-time (#1361) and PTA (#1362) are advanced/deferred. (§17)
- **Weighted start states.** `start_states: [a 30% b 70%]` = initial distribution μ₀; companion to weighted transitions → full Markov chain. **Unifying frame (lead with this):** it's the *same weighted-`LabelList`* as the probabilistic list target, seen from **two positions** — an **arrow target** has an origin, so two levels (group `%` × member `%`); a **start set** has no origin, so one level (members *are* μ₀). One rule everywhere: members carry optional `%`, uniform if omitted, normalized in context. (§17)
- **Random walks & statistical MC.** Seeded reproducible walks; statistical model checking (works on rich-tier); coverage; rare-event/importance sampling. (§17)
- **Scaling the checker (teach the three-rung ladder explicitly).** Native exhaustive (small machines) → **assume-guarantee compositional** (contracts compose, state spaces don't — teach with the 500-customer park: one contract proof, not a 500-fold product) → **CEGAR** (aspirational; abstract, then refine where the spurious counterexample lied). Also the *direction* ladder: `infer` learns from behavior, `check` judges against properties, `synth` derives from properties. (§23/§28)
- **Flow contracts (the security chapter's second half, after guardrails).** `disallow flow pin -> log;` — teach with the PIN/telemetry example; guardrails constrain actions, flow contracts constrain data; relate to `--redact` (read-side vs full lattice). *Gotcha:* flow is checked over FSL semantics — a host hook that receives the secret is an *exit* from the proof; audit hook signatures. (§10/§17)
- **SLO contracts.** PCTL in contract position: the same property is the design-time proof (Storm backend) and the production drift monitor — "your SLO is a contract on the machine, not a paragraph in a runbook." (§17)
- **Composed-system checking.** What `finite` means for a *system* (all members finite + population `max` + queue bound — finite **by construction**); the five checks (bounds, lifted contracts, deliverability, system `invariant` over `count`/`all`/`any`/`sum` aggregates, opt-in deadlock). *Teach:* **counterexamples are executable** — the checker hands you a stimulus tape that `fsl run` replays; checker, stochastic tester, and factory are one machine. *Gotcha:* aggregates are system-context-only builtins. (§23)
- **The user testing toolkit (must be its own chapter).** Unit assertions (`reaches`/`blocked`/`reachable`/property — a unit test can be a *model-check over all paths*); stochastic tools; **shrinking** to minimal failing trace; **model-based testing** (the machine as oracle for a real system — `val`s=shadow state, `require`/`ensure`=pre/post, named hooks=real ops — the John Hughes / QuickCheck story); concolic guidance (SMT-steered walks). **Authoring surface:** in-language `test`/`expect` blocks (declarative, verifiable, codegen-exported) + host API; vocabulary (reaches/blocked/invariant-holds/contract-never-violated/temporal/IO-relation/replay-equivalence/coverage); **tests are tapes** (shrunk failing walk → permanent regression); coverage = state/transition/path/guard MC-DC. *Teach the cache/bank worked examples.* (§17)

## Part VII — Tooling & integration
- **Layout sugar `arranged`.** `arranged a -> b -> c;` = the chain *plus* `arrange [a b c]` — DRY for the "write the sequence, then write the shape" annoyance. *Teach:* desugaring, decorations stay on edges, best for linear chains, repeats dedup; `arranged-start`/`arranged-end` analogs.
- **Template interpolation vs the expression language (teach the phase boundary).** `sm`/`ms` interpolation (`` sm`a ${x} -> b` ``) is **build-time host (JS) text-splicing** into the FSL source before parse (jssm.ts:5017) — untyped, unhygienic, JS-only, and **gone after codegen** (Rust/C/Python have no tagged templates). The **expression language** (vals/sensors/guards) is **run-time, host-agnostic, typed, verifiable, replayable**. *Rule:* **interpolate to BUILD a machine** (generate structure from host data, machine families, host-computed constant tables — the escape hatch the bounded expression language deliberately won't provide); **use a val/sensor/expression to make it BEHAVE dynamically** (probabilities, thresholds, defaults). *Gotchas:* interpolating a runtime value bakes one JS snapshot into source (loses typing/verification/replay/portability); interpolating untrusted input is **FSL injection** (string-injection into the grammar) — use a typed val. *Verification symmetry:* build-time interpolation keeps a `finite` machine fully concrete for the checker; runtime vals/sensors add the input dimension it must over-approximate. (§14/§15)
- **Visualization & CSS styling.** Stable SVG **IDs** + semantic **classes** (`start-state`/`end-state`/`error-state`/`hooked-state`/**live `current-state`**/`fsl-group-*`) + CSS **`::part()`** for shadow-DOM web components; names are **slugified** so use the API (`svg_id_for_state`/`svg_id_for_edge`/`svg_element_map()`). *Gotcha:* you can't guess the selector from the state name. (decision log)
- **Multi-host & conformance.** Targets (rust/c/python/php/ruby/c#/js now; lisp/erlang/elixir + dozens); what's portable vs implementation-defined (regex, locale casing/normalize); the conformance suite as the contract. (§16)
- **Codegen output = four artifacts.** (1) runtime, (2) the machine's **type surface** exported host-idiomatically (state/action/tape alphabets, val·sensor·prop types, ADTs, enums, records, Error/option — C header enums, TS types/interfaces, Rust enums, Python Enum/dataclass), (3) the **hook interface** to implement (TS interface / Rust trait / C fn-ptr struct), (4) the **package manifest**. *Why:* the type surface is the compile-time half of the host↔machine contract — named hooks (runtime half) need the machine's types to be implemented type-safely. *Gotcha:* "in languages where it makes sense" — dynamic hosts get constants/hints/docblocks; identifiers are slugified per host. (§16)
- **Package-name attributes.** General `package_name` (canonical base) + per-manager overrides `npm_name`/`cargo_name`/`pip_name`/`nuget_name`/`composer_name`/`gem_name` (+ `jsr_name`/`conda_name` as needed); full ecosystem name, override > derived. *Decision:* named for the **package manager, not the language** (`npm` not `node`; `pip` not `python`) — extensible per-manager, the axis where names diverge. *Why:* naming conventions diverge per ecosystem (`@acme/x` vs `Acme.X` vs `acme/x`); one name can't serve all. Codegen emits the matching manifest. (§16)
- **The CLI (its own manual part — 21 verbs, teach by workflow not alphabetically).** The four template tags (`fsl`/`fslf`/`fss`/`fssf`; `sm` deprecated) and their CLI counterparts. *Teach the flows, not the flags:* author (`init` → `lint`/`format` → `render --watch`, or just `lsp` in an editor), test (`test`, `repl` — **a repl session saves as a regression test**), fuzz (`walk` — statistical MC, coverage-guided generation, **shrunk failures become `test` files**), verify (`check` → counterexample file → `run` replays it — two-command reproduction; `inspect` reads any bundle first), ship (`codegen` → `--certify` → `publish`), agents (`mcp` — the AI-facing twin of `lsp`). *Gotchas:* `import`-the-verb converts formats, it does NOT fetch packages (host package managers do); `publish` never touches a registry without confirmation; the publish bundle auto-includes README + HTML/PNG/SVG renders with FSL attribution links; no `migrate` — v5 documents parse under v6 (context-scoped keywords), revisit at v7. (§25)
- **Conformance tiers (for codegen-target authors).** Vectors-as-data; the thin-harness contract (~200 lines per host); T1 finite / T2 rich-portable / T3 pinned-unicode; capability manifests; differential mode. *Teach:* "port the harness, not the suite." (§26)
- **Writing FSL with AI (its own chapter — teach both directions).** For humans using AI: the ~500-token cheat-sheet, the Claude plugin, the grammar artifacts for constrained decoding, and *why* AI+FSL beats AI+general-purpose code — the checker catches plausible-but-wrong; demo the full repair loop end-to-end (agent writes → `check --json` → counterexample tape → replay → fix → proof). For agent builders: the `--json` verb contracts (spans + `suggested_fix`), non-interactive totality, **budgeted verification** (`undecided` is a real answer — teach the tri-state: pass / fail-with-tape / undecided-with-partials), `docs --interface` (compose against interface cards, not sources, when context is tight), `explain` codes, `init --example` (edit valid artifacts, don't generate from blank), `run --require-profile pure` (verify-before-run: statically provable harmlessness for untrusted machines), `list` + `--capabilities` discovery, `mcp`, `render --target ascii` (text-only models can *see* the graph), the eval set. *Gotchas:* `--json` stdout is the API — human prose goes elsewhere, never parse the prose; treat `undecided` as its own branch, not as failure; `suggested_fix` is a suggestion — re-`check` after applying, never chain-apply blind; a verification certificate is dead the moment the source or checker version changes — check the hash before trusting it. (§25/§25.1)
- **Verification certificates (teach as the trust story's capstone).** What a certificate attests (this hash, these properties, this checker, this tier); what it does NOT attest (anything about edited copies, host hook implementations, or rich-tier behavior outside the checked properties); the publish bundle carries it; CI verifies without re-running; re-certification is one command. *Teach the agent-economy frame:* when machines move between systems and authors — human or AI — the certificate is what trust is denominated in. (§25)
- **Tape forensics.** `diff` over two runs (first divergence step), `--narrate` for prose traces, `mcp` time-travel tools (`state_at`/`why_rejected`/`val_history`), **counterfactual replay** (`run --amend` — vary one thing, observe the fork; the causal tool debuggers can't have because ordinary programs don't replay) — teach debugging as *interrogating history* rather than stepping a live process; total replay makes the tape the only artifact you need. (§25)
- **Redaction & sharing repro bundles.** `inspect --redact` and the verifier-emitted redaction map: provably behavior-irrelevant fields pseudonymize freely (the bundle still replays bit-identically); load-bearing fields get flagged. *Teach the insight itself:* "is this field safe to redact?" is a static question with a proof, not a judgment call. *Gotcha:* the map covers guard/selector reads only — a host hook that logs a val is outside the proof; audit named-hook signatures before sharing. (§25)
- **Simulation & digital twins (its own chapter — a market, not a feature).** `walk --sweep` over factory bindings = experiment design; the RCT park run at every arrival rate; teach: model → sweep → analytics → *then* build, with every cell reproducible by coordinate. Position against SimPy/AnyLogic: same workflow, plus proofs.
- **Safe evolution in CI.** `verify` (dependency certificates), `test --impacted` (exact impact analysis), `diff --notes` (behavioral release notes), `minimize` (the design-smell mirror) — teach as one workflow: change → impacted tests → refinement verdict → generated notes → certified release. *Teach bisimilarity via the `minimize` button:* "the same machine" has a definition, and here's the command that computes it.
- **Durable execution & drift detection (the production chapter).** Machines + tapes ARE durable workflows: persistence contract, resume-after-crash = rehydrate + replay tail; certificates make them *verified* workflows (the Temporal comparison, made explicitly). Drift detection: the live tape's statistics vs the model's predictions — the same document is spec, implementation, oracle, and anomaly detector; teach that sentence. Live editor verification (`lsp` check daemon) closes the loop at the other end: proofs while typing, proofs in production. (§15/§17)
- **CI integration.** The FSL GitHub Action (lint/check/certify, PR comments with diagram + semantic diff + refinement verdict); `test --doc` keeps every published snippet honest — teach "your docs are part of your test suite" (the same principle as the repo's executable docblocks, third layer). `check --estimate` before `check` on big systems. (§25)
- **Verified agent guardrails (its own chapter — possibly the book's closer).** `mcp --policy guard.fsl`: the agent's tool surface gated by a certified machine — which tools in which states, legal payloads, forbidden sequences; "never `charge` twice without `confirm`" as a *proven* property instead of a system-prompt plea. *Teach:* guardrails-as-vibes vs guardrails-as-artifacts; write the policy machine, model-check it, watch the MCP server enforce it; the policy is small, visual, diffable, and certifiable. *This is FSL securing the interface that exposes FSL to AI — say so explicitly; it's the book's best closing argument.* (§25)
- **Checker backends & researcher bridges.** `check --via` (nuXmv/Spin/Storm) and `export --target tla+/alloy` — teach "FSL semantics are FSL's; the backend is an engine, and engines must agree" (differential checking). For readers coming FROM formal methods: the bridges are your on-ramp in. (§17)
- **The FSL Claude plugin is a CONSUMER of this file.** Its `SKILL.md` (teaching Claude idiomatic FSL) maps 1:1 onto this syllabus: the mental-model section ← the cross-cutting principles; pitfalls-Claude-gets-wrong ← the Gotchas appendix; tooling commands ← the CLI part; worked snippets ← the examples appendix. When writing the skill, mine this file first; when this file grows a gotcha, the skill likely needs it too. The plugin's MCP component is just `.mcp.json` → `fsl mcp`; its trigger description fires on `.fsl` files / FSL/jssm mentions. (megaspec §25)

---

## Appendix — Gotchas / migration checklist (collect these for a "things that will surprise you" page)
- `var`→`val`, `property`→`prop` (alias removed v7); `set` keyword → `assign`.
- Bracket-index **throws**; `get` returns `option`. `null = undefined` is **false**; no `null` in arithmetic.
- `any` is **sound-narrow-only** (must `case`). Bitwise on **sized** ints only.
- Units: **torque ≡ energy** (use branding), **affine** temperature, **KB≠KiB**.
- The **microstep bound** (default 100,000/reaction) catches eventless-loop hangs.
- Multigraph: probabilistic vs unlabeled distinction; `transition(to)` needs an unlabeled edge.
- Reserved words are **context-scoped** (your state named `case` still works outside expressions).
- `finite` machines **forbid** the rich tier (float/double, longint, open containers, recursive ADTs, first-class lambdas, `any`, unbounded) but **allow** over-approximated `rand`/`sensor`; `data` forbidden.
- SVG ids are **slugified** — fetch them from the API.
- Compression encoders are **pinned** (don't expect host-native zlib byte-compat).
- Factory `seq()` indices are **never reused** after `retire`; `make()` moves the cursor, `at(n)` doesn't.
- Unseeded constructions **still have a seed** (clock-sampled once at the root, recorded) — read it back with `.seed()` before claiming irreproducibility.
- `on_undeliverable` defaults to **error**; if your park "randomly crashes," a route is delivering to a state that doesn't accept the action — `dead_letter` is the diagnostic mode.
- System containment must be **acyclic**; wiring may cycle. If the compiler rejects your composition, you wanted `wire`, not a member.
- GoF update: with document factories + `spawn`, **Factory Method / Abstract Factory are no longer out of domain** — revise the Part-I GoF list when writing it.

## Appendix — Worked examples to reuse (already written this session)
Battery charger (product units), cruise control (rate units), download manager (data units, bits/bytes), rocket ascent controller (extreme units), risk monitor (square dollars = variance), motor controller (mechanical+electrical watts, torque/energy twist), raptor startup (arbitrary units), key-value/bank (model-based testing), **Paxos consensus** (Systems + message payloads + the prop/val/sensor trichotomy *live* — proposer = control machine, acceptor = data machine; `invariant forall x,y in chosen : x=y` = the agreement safety property; **unbounded ballots ⇒ register/data automaton ⇒ `checkable`/SMT tier, not `finite`**; safety passes while liveness needs a leader — the safety-vs-liveness split; "executable, visual TLA+" made concrete). **Chess** (the flagship for the **verifiability tiers** — chess is a **data machine**: the board is a `val`, the only control states are `playing` + checkmate/stalemate/draw; the **rules ARE contracts** — move legality = the `where` guard, king-safety = `ensure`, two-kings-impossible = `invariant`; Piece/Square/Move **ADTs** + `case` move-generation, or a function-typed-val **Strategy per piece**; *THE lesson:* **finite-but-not-enumerable** — ~10⁴³ positions, so it lands in `checkable` / large-finite-SMT, **not** small-finite-enumerable — and a **depth-bounded model-check over it *is* a chess engine** ("mate in 3" = bounded reachability; the game tree = a `tree` / game automaton); the move history **is** the input tape, **PGN/FEN** = serialization, replay reconstructs the board (a famous game = a regression test); the "weird rules" — castling / en-passant / 50-move / threefold — are exactly where the auxiliary vals + typed `Move` ADT earn their keep).

**The reference repair-loop recipe (write this as a cookbook entry AND a manual section).** ~50 lines implementing the full agent harness: generate → `check --json` → narrate the counterexample → repair → re-check, with the budget tri-state handled correctly and `source_hash` memoization. Agent builders copy working loops far more often than they read contracts; this is the §25 contracts made executable, and it doubles as the integration test for the whole agent feature set.

**Systems / protocols / classic verification examples** (recognizable, exercise Systems + contracts + verification — pick per feature being taught):
- **Consensus & distributed.** **Raft** (leader election + log replication — the *understandable* consensus; follower/candidate/leader is a clean control machine; terms, log-match safety) — pairs with the **Paxos** entry above; **two-phase / three-phase commit** (atomic commit — coordinator + participants; the blocking problem); **leader election** (Bully, Chang–Roberts ring); **Lamport / vector clocks** (logical time — val-as-counter, happens-before ordering); **Chandy–Lamport snapshot**; **gossip / epidemic dissemination** (probabilistic → statistical MC).
- **Concurrency & synchronization.** **Dining philosophers** (the deadlock / liveness / starvation classic — a `System` of philosophers + fork resources); **Peterson's** & **Lamport's bakery** mutual exclusion (*the* intro model-checking examples — safety: mutual exclusion, liveness: no starvation); **readers–writers**; **producer/consumer bounded buffer** (the `petri`/VASS worked example, with semaphores).
- **Network protocols.** **TCP connection lifecycle** (the textbook protocol state machine); **alternating-bit protocol** (*the* classic protocol-verification example over a lossy channel); **sliding-window / go-back-N ARQ**; **TLS handshake** (session types).
- **Classic FSM on-ramps (gentle).** **Turnstile** (minimal); **vending machine** (the extended-state intro — `val balance`); **traffic light** single + coordinated-intersection (the latter = Systems + shared clock); **elevator controller** (requests as vals + scheduling).
- **The automata rungs, illustrated.** **regex matcher** (`regular`); **balanced parentheses** (`pushdown`, one stack); **bracket / XML validator** (`tree`); **Conway's Game of Life** (cellular automata — a `System` of cells + synchronous clock + columnar batch-stepping); **behavior tree** (game AI — a `tree` ticked synchronously).
- **Security & authentication protocols (verification showcases).** **Needham–Schroeder** (*the* famous example where model checking found a 17-year-old protocol flaw — Lowe's attack; teach "verification finds real bugs"); **Kerberos**; **Diffie–Hellman key exchange**; **OAuth 2.0 / OIDC** flow (recognizable to web devs); **two-generals / coordinated attack** (the *impossibility* classic — the checker proves **no** protocol satisfies it; teach the negative result).
- **Hardware, embedded & control.** **MESI / MOESI cache coherence** (the multiprocessor model-checking classic — Modified/Exclusive/Shared/Invalid); **UART / SPI / I²C** bus protocols (bit-level FSMs); **CPU fetch–decode–execute** cycle; **thermostat** (bang-bang — the canonical embedded FSM); **microwave / washing-machine** controllers; **railway interlocking** (safety-critical formal methods — *prove no two trains collide*); **DHCP lease** lifecycle.
- **OS & concurrency (more puzzles).** **process / thread lifecycle** (ready/running/blocked/terminated); **banker's algorithm** (deadlock avoidance); **sleeping barber** & **cigarette smokers** (the other classic synchronization puzzles, beside dining philosophers); **page / TLB states**; **Treiber stack / lock-free queue** (concurrent data structures).
- **Business, web & workflow.** **shopping-cart / checkout**; **order fulfillment & shipping**; **subscription billing** (active / past-due / grace / canceled); **issue / ticket lifecycle** (open→in-progress→resolved→closed); **CI/CD pipeline** (build→test→deploy); **git** working/staged/committed; **document-approval workflow** (BPMN-flavored); **feature-flag / experiment rollout**.
- **Games, AI & simulation.** **The Roller Coaster Tycoon park** (THE factories/systems flagship, straight from fsl#431/#459: a customer factory with `seq()` names + `rand()` patience spawning a population, ride machines, a gate, routes + `spawn`/`retire` — teaches factories, populations, routes, lifecycle, dead-letter, and composed checking in one running example; build it incrementally across the manual); **Pac-Man ghost AI** (chase / scatter / frightened — the beloved game FSM); **Mario power-up** states (small/big/fire/star); **turn-based game** state (chess / tic-tac-toe — a game tree); **Nagel–Schreckenberg traffic** (cellular-automaton traffic flow); **tamagotchi / virtual pet** (vals for hunger/health — fun extended-state).
- **Science & populations.** **SIR / SEIR epidemic** model (compartmental — probabilistic/population, statistical MC; timely & recognizable); **chemical reaction networks** (`petri` — species = places, reactions = transitions); **cell cycle / mitosis** stages; **predator–prey** (discretized Lotka–Volterra); **gene-regulatory / boolean networks**.
- **Databases & storage.** **MVCC / transaction isolation** levels (read-committed → serializable); **write-ahead log & crash recovery** (ARIES); **CRDTs** (G-counter / OR-set — eventual consistency via commutative merge); **distributed lock / lease** (Chubby / ZooKeeper); **saga pattern** (compensating transactions across microservices).
- **Fintech.** **payment authorization lifecycle** (authorized → captured → settled → refunded → chargeback — the card/Stripe flow); **limit-order lifecycle** (order book — new / partial-fill / filled / canceled; *invariant: never overfill*); **double-entry ledger** (the **`invariant debits = credits`** showcase — contracts at their best); **ATM transaction** (card → PIN → withdraw → dispense → eject — fintech meets embedded); **loan / mortgage underwriting** (application → underwriting → approved/denied → funded → servicing — a BPMN workflow); **margin call / liquidation** (healthy → margin-call → liquidating → closed — pairs with the risk-monitor units example).
- **Appliances.** **coffee / espresso machine** (heat → brew → ready → descale); **dishwasher cycle** (pre-rinse → wash → rinse → dry); **garage door** (open / closing / closed / opening / **obstructed** — the obstruction *sensor*); **electric kettle** (boil → keep-warm → auto-off); **robot vacuum / Roomba** (docked → cleaning → returning → stuck → charging — sensors + docking); **smart thermostat** (heat / cool / eco modes + schedule).
- **Gaming.** **player character controller** (idle / run / jump / fall / attack / hurt / dead — every platformer); **enemy AI** (patrol → alert → chase → attack → search → return — stealth AI; pairs with behavior trees); **weapon cycle** (ready → firing → reloading → empty — the FPS classic); **quest / mission state** (not-started → active → completed / failed / turned-in); **matchmaking / lobby** (searching → match-found → loading → in-game → post-game); **Tetris piece** (spawn → falling → locked → line-clear); **dialogue tree** (a `tree`).
- **Strategy.** **iterated prisoner's dilemma / tit-for-tat** (*a strategy in a repeated game **is** a finite automaton* — Axelrod's tournaments literally pitted FSMs against each other; the purest "strategy = machine" example, and tit-for-tat / grim-trigger / Pavlov are all tiny Mealy machines); **tech tree / build order** (a **DAG** of unlock dependencies — 4X / RTS); **RTS unit command** (idle → move → attack-move → engage → retreat); **tower defense** (wave spawner + tower / enemy states); **board-game turn phases** (Catan / Monopoly / Risk — roll → move → build → trade); **auction mechanisms** (English / Dutch / Vickrey bidding rounds — also fintech); **minimax game-tree** (the decision procedure — game automata).
- **Also named earlier this session (capture these too).** **PBFT / Byzantine fault tolerance** (consensus under malicious nodes); **Chord / Kademlia DHT** (distributed-hash-table routing & lookup); **physical clock synchronization** (NTP / Berkeley / Cristian's — distinct from the *logical* Lamport/vector clocks above); the **`Expr` sign/parity tree automaton** (the concrete `tree`-machine recognizer — each node's state from its children's); the **FSL self-hosting compiler pipeline** (lexer=`local` → parser=`pushdown` → checker=`tree` → passes/codegen=`tree` transducers — the model-class ladder dogfooded; StoneCypher/fsl#1363); the **Anti-Life Equation** (contradictory `invariant`s → **UNSAT** → unreachable transition → *ex falso quodlibet*; the memorable "what an unsatisfiable contract actually does" lesson); the **"unhinged" units escalation** (invented base dimensions + the SI prefix system on them + cube-roots / rational exponents — the extreme dimensional-algebra demo, escalated from the raptor example).
