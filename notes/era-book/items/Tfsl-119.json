{
  "id": "Tfsl-119",
  "title": "spec-in-PEG audit",
  "version": 10,
  "cluster": "conformance",
  "src": [
    "e1-triage",
    "fsl#119"
  ],
  "issue": 119,
  "manager": "Audit the PEG grammar against the language specification to find where the implemented grammar and the written spec have diverged — the C3-adjacent check that the parser actually parses the language the spec describes. Grammar and spec drift apart silently; an audit is how you catch the clauses where the parser is stricter, looser, or just different from what the document claims."
}
