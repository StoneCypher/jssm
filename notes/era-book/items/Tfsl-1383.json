{
  "id": "Tfsl-1383",
  "title": "editor codegen menu",
  "version": 10,
  "cluster": "editor-qol",
  "src": [
    "e1-triage",
    "fsl#1383"
  ],
  "issue": 1383,
  "manager": "A menu in the web-component editor that generates code from the current machine — TypeScript types, a renderer embed, a CLI invocation — so a user can go from diagram to usable artifact without leaving the editor. It's the bridge from 'I drew a machine' to 'I have code', which is where most authoring sessions actually want to end."
}
