{
  "id": "Tfsl-52",
  "title": "lint missing annotations",
  "version": 10,
  "cluster": "lint",
  "src": [
    "e1-triage",
    "fsl#52"
  ],
  "issue": 52,
  "manager": "A lint check for missing annotations — states or transitions that should carry documentation, labels, or metadata but don't — so gaps in a machine's self-description get flagged rather than silently accumulating. It's the lint rule that keeps a machine's annotation discipline from eroding one un-annotated state at a time."
}
