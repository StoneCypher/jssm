{
  "id": "Tfsl-752",
  "title": "richer error object — model umbrella",
  "version": 9,
  "cluster": "error-model",
  "src": [
    "e1-triage",
    "fsl#752"
  ],
  "issue": 752,
  "manager": "The §11 umbrella for turning FSL's thrown error from a bare message into a structured object a program can actually reason about. Today an error tells you little more than that something failed; the goal is an object carrying kind, location, and machine context so callers can branch on failure programmatically instead of string-matching."
}
