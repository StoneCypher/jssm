{
  "id": "Tfsl-509",
  "title": "TS definitions as strings",
  "version": 10,
  "cluster": "typegen",
  "src": [
    "e1-triage",
    "fsl#509"
  ],
  "issue": 509,
  "manager": "Emit the generated TypeScript `.d.ts` content as a string from the API, so a build script or tool can capture the types without going through a file on disk. It's the programmatic hook that lets typegen slot into arbitrary build pipelines rather than only the CLI's file-writing path."
}
