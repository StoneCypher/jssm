{
  "id": "Tfsl-704",
  "title": "Automate cdnjs updates in CI",
  "version": 16,
  "cluster": "distribution-listings",
  "src": [
    "e7-triage",
    "fsl#704"
  ],
  "issue": 704,
  "manager": "Make refreshing the cdnjs entry a CI/CD step rather than a manual chore, so the CDN never lags the npm release. Stale CDN copies are a silent trust-killer — someone tries the 'latest' browser build, hits a bug fixed months ago, and leaves; automating the bump closes that gap permanently."
}
