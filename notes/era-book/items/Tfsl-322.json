{
  "id": "Tfsl-322",
  "title": "viz visual-surface META (state/edge/graph)",
  "version": 10,
  "cluster": "viz",
  "src": [
    "e1-triage",
    "fsl#322"
  ],
  "issue": 322,
  "manager": "The organizing META for the three visual surfaces of a rendered machine — how states look, how edges look, how the overall graph lays out — so styling work is planned per surface instead of ad hoc. It's the map that tells you whether a given request is a node concern, an edge concern, or a layout concern, which is exactly the axis a theme system needs.",
  "cs": "folds fsl#319 (state visual), #320 (edge visual), #321 (graph visual)"
}
