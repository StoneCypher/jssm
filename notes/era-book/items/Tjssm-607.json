{
  "id": "Tjssm-607",
  "title": "theme files + auto-load (was jssm#607)",
  "version": 10,
  "cluster": "themes",
  "src": [
    "jssm-drain",
    "jssm#607"
  ],
  "issue": 607,
  "manager": "Support standalone theme files that the CLI and viewer auto-discover and load, so a project can drop a theme next to its machines and have every render pick it up. It's the mechanism that turns theming from a per-invocation flag into a project-level default."
}
