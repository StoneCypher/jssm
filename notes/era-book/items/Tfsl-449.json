{
  "id": "Tfsl-449",
  "title": "reset semantics + constructor args",
  "version": 9,
  "cluster": "reset-semantics",
  "src": [
    "e1-triage",
    "fsl#449"
  ],
  "issue": 449,
  "manager": "Define precisely what `.reset()` restores — initial state, initial data, cleared tapes — and let reset optionally take the same constructor arguments so you can reset a machine into a fresh configuration rather than only its birth state. This matters for long-lived machines and test harnesses that re-run a machine thousands of times and need a crisp, documented notion of 'back to start'.",
  "cs": "folds fsl#1243 (reset takes constructor args)"
}
