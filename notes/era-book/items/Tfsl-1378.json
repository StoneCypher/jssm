{
  "id": "Tfsl-1378",
  "title": "SemVerRange compatibility attribute",
  "version": 10,
  "cluster": "serialize",
  "src": [
    "e1-triage",
    "fsl#1378"
  ],
  "issue": 1378,
  "manager": "A machine attribute declaring the range of FSL versions it's compatible with, expressed as a SemVer range, so a loader can decide up front whether it can safely handle the machine. It's the compatibility contract that makes version-gated deserialization precise: the machine states its own requirements instead of the loader guessing."
}
