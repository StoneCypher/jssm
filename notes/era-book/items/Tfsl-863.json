{
  "id": "Tfsl-863",
  "title": "Family dependency automation (umbrella)",
  "version": 16,
  "cluster": "automation",
  "src": [
    "e7-triage",
    "fsl#863"
  ],
  "issue": 863,
  "manager": "The umbrella for automating dependency propagation across the FSL family — when the core publishes, the downstream packages that depend on it update automatically. It exists to kill the manual, error-prone chore of hand-bumping a dozen sibling repos every release, which is exactly where stale-version bugs breed."
}
