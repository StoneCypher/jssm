{
  "id": "Tfsl-1274",
  "title": "matrix: Node (cjs / static / dynamic)",
  "version": 10,
  "cluster": "build-matrix",
  "src": [
    "e1-triage",
    "fsl#1274"
  ],
  "issue": 1274,
  "manager": "Cover the Node.js import paths — CommonJS require, static ESM import, and dynamic import() — as distinct matrix legs, because a package can pass one and silently break another. Node's module story has enough sharp edges (dual-package hazard, conditional exports) that each entry path genuinely needs its own proof.",
  "cs": "folds fsl#1273 (node cjs), #1275 (node dynamic)"
}
