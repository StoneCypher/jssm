# Issue Management Rules

- **Check Both Repositories**: Whenever querying, linking, managing, or interacting with issues, you MUST always check both the `StoneCypher/jssm` and `StoneCypher/fsl` repositories. The issues are split between the two codebases.
