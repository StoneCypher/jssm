#!/usr/bin/env node
"use strict";var node_fs=require("node:fs");var node_path=require("node:path");var promises=require("node:fs/promises");var node_os=require("node:os");var Ajv=require("ajv");var jssmFence=require("jssm-fence");var jssmViz=require("jssm-viz");function parseFslArgs(argv,spec){const positional=[];const flags={};const shortMap={};for(const[name,fs]of Object.entries(spec.flags)){if(fs.short)shortMap[fs.short]=name}const isBoolean=name=>spec.flags[name]?.boolean;const flagType=name=>{const fs=spec.flags[name];return fs.type??"string"};const coerce=(name,raw)=>{const t=flagType(name);if(t==="number"){const n=Number(raw);if(Number.isNaN(n)){throw new TypeError(`flag --${name} requires a number, got: ${raw}`)}return n}const fs=spec.flags[name];if(fs.enum&&!fs.enum.includes(raw)){throw new Error(`flag --${name} value '${raw}' not in: ${fs.enum.join(", ")}`)}return raw};const missingValue=idx=>idx>=argv.length||argv[idx].startsWith("--");let i=0;let positionalOnly=false;while(i<argv.length){const a=argv[i];if(positionalOnly){positional.push(a);i++;continue}if(a==="--"){positionalOnly=true;i++;continue}if(a==="-"){positional.push(a);i++;continue}if(a.startsWith("--")){const eq=a.indexOf("=");const name=eq===-1?a.slice(2):a.slice(2,eq);if(!Object.prototype.hasOwnProperty.call(spec.flags,name))throw new Error(`unknown flag: --${name}`);if(isBoolean(name)){flags[name]=true;i++}else if(eq===-1){if(missingValue(i+1))throw new Error(`flag --${name} requires a value`);flags[name]=coerce(name,argv[i+1]);i+=2}else{flags[name]=coerce(name,a.slice(eq+1));i++}continue}if(a.startsWith("-")&&a.length>1){const short=a[1];const name=shortMap[short];if(!name)throw new Error(`unknown flag: -${short}`);if(isBoolean(name)){flags[name]=true;if(a.length>2)throw new Error(`combined short flags not supported: ${a}`);i++}else if(a.length>2){flags[name]=coerce(name,a.slice(2));i++}else{if(missingValue(i+1))throw new Error(`flag -${short} requires a value`);flags[name]=coerce(name,argv[i+1]);i+=2}continue}positional.push(a);i++}for(const[name,fs]of Object.entries(spec.flags)){if(flags[name]===void 0&&fs.default!==void 0){flags[name]=fs.default}}const helpText=()=>{const lines=["Usage:","  "+spec.usage,"","Options:"];for(const[name,fs]of Object.entries(spec.flags)){const short=fs.short?`-${fs.short}, `:" ".repeat(4);const longPart=`--${name}`;let arg=" VALUE";if(fs.boolean){arg=""}else if(fs.enum){arg=` ${fs.enum.join("|")}`}else if(fs.type==="number"){arg=" N"}const defStr=fs.default===void 0?"":` (default: ${fs.default})`;lines.push(`  ${short}${longPart}${arg}${defStr}`)}return lines.join("\n")};return{positional:positional,flags:flags,helpText:helpText}}const isPlainObject=v=>typeof v==="object"&&v!==null&&!Array.isArray(v)&&Object.getPrototypeOf(v)===Object.prototype;const mergeTwo=(a,b)=>{if(b===void 0)return a;if(b===null)return null;if(isPlainObject(a)&&isPlainObject(b)){const out={...a};for(const[k,value]of Object.entries(b)){const merged=mergeTwo(a[k],value);if(merged===void 0){delete out[k]}else{out[k]=merged}}return out}return b};function mergeConfigs(layers){let acc={};for(const layer of layers){acc=mergeTwo(acc,layer)}return acc}const deepFreeze=o=>{if(o&&typeof o==="object"){for(const value of Object.values(o)){deepFreeze(value)}Object.freeze(o)}return o};const defaults=deepFreeze({include:["**/*.fsl"],exclude:["**/node_modules/**"],render:{defaultTarget:"svg",scale:3,quality:85},lint:{},format:{},test:{},check:{},typegen:{},codegen:{},init:{},import:{},export:{},mcp:{},lsp:{},repl:{},registry:{}});class ConfigError extends Error{constructor(message,opts={}){super(message);this.name=this.constructor.name;this.path=opts.path;this.line=opts.line;this.column=opts.column}}class ConfigParseError extends ConfigError{constructor(message,opts={}){super(message,opts);this.kind="parse";Object.setPrototypeOf(this,ConfigParseError.prototype)}}class ConfigSchemaError extends ConfigError{constructor(message,opts){super(message,opts);this.kind="schema";this.violations=opts.violations;Object.setPrototypeOf(this,ConfigSchemaError.prototype)}}class ConfigExtendsError extends ConfigError{constructor(message,opts){super(message,{path:opts.path});this.kind="extends";this.chain=opts.chain;Object.setPrototypeOf(this,ConfigExtendsError.prototype)}}class ConfigIOError extends ConfigError{constructor(message,opts){super(message,{path:opts.path});this.kind="io";this.errno=opts.errno;Object.setPrototypeOf(this,ConfigIOError.prototype)}}const emptySection={type:"object",additionalProperties:true};const CONFIG_SCHEMA={$schema:"http://json-schema.org/draft-07/schema#",$id:"https://stonecypher.github.io/jssm/schemas/fsl-config.json",title:"fsl Configuration",description:"Unified configuration for the fsl CLI and the jssm/cli library.",type:"object",additionalProperties:false,properties:{$schema:{type:"string"},extends:{oneOf:[{type:"string"},{type:"array",items:{type:"string"}}]},include:{type:"array",items:{type:"string"}},exclude:{type:"array",items:{type:"string"}},render:{type:"object",additionalProperties:false,properties:{defaultTarget:{type:"string",enum:["svg","dot","png","jpeg","html"]},outDir:{type:"string"},scale:{type:"number"},width:{type:"number"},height:{type:"number"},quality:{type:"number"},theme:{type:"string"}}},lint:emptySection,format:emptySection,test:emptySection,check:emptySection,typegen:emptySection,codegen:emptySection,init:emptySection,import:emptySection,export:emptySection,mcp:emptySection,lsp:emptySection,repl:emptySection,registry:{type:"object",additionalProperties:{type:"string"}}}};const ajv=new Ajv({allErrors:true});const validator=ajv.compile(CONFIG_SCHEMA);function validateConfig(config,opts={}){const ok=validator(config);if(!ok){const violations=validator.errors;const summary=violations.map(v=>`${v.instancePath||"/"}: ${v.message}`).join("; ");throw new ConfigSchemaError(`config schema violation${opts.path?` in ${opts.path}`:""}: ${summary}`,{path:opts.path,violations:violations})}}const MAX_DEPTH=32;const dirnameOf=path=>{const ix=Math.max(path.lastIndexOf("/"),path.lastIndexOf("\\"));return ix===-1?".":path.slice(0,ix)};const joinPath=(dir,rel)=>{if(/^(?:[a-z]:)?[/\\]/i.test(rel))return rel;const combined=dir+"/"+rel;const parts=combined.split(/[/\\]+/);const out=[];for(const p of parts){if(p==="."||p==="")continue;if(p==="..")out.pop();else out.push(p)}const prefix=/^[a-z]:/i.test(combined)?"":"/";return prefix+out.join("/")};const omitKey=(obj,key)=>{const out={};for(const[k,value]of Object.entries(obj))if(k!==key)out[k]=value;return out};async function resolveExtends(raw,basePath,reader,visited=[]){if(visited.includes(basePath)){throw new ConfigExtendsError(`extends cycle detected: ${[...visited,basePath].join(" -> ")}`,{path:basePath,chain:[...visited,basePath]})}if(visited.length>=MAX_DEPTH){throw new ConfigExtendsError(`extends depth ${MAX_DEPTH} exceeded`,{path:basePath,chain:[...visited,basePath]})}if(!raw.extends){return omitKey(raw,"extends")}const extendsList=typeof raw.extends==="string"?[raw.extends]:raw.extends;const baseDir=dirnameOf(basePath);const nextVisited=[...visited,basePath];const bases=[];for(const rel of extendsList){const absPath=joinPath(baseDir,rel);const text=await reader(absPath);let parsed;try{parsed=JSON.parse(text)}catch(error){throw new ConfigParseError(`malformed JSON in ${absPath}: ${error.message}`,{path:absPath})}validateConfig(parsed,{path:absPath});const resolved=await resolveExtends(parsed,absPath,reader,nextVisited);bases.push(resolved)}const ownKeys=omitKey(raw,"extends");return mergeConfigs([...bases,ownKeys])}const fsReader=async path=>{try{return await promises.readFile(path,"utf8")}catch(error){const errno=error;throw new ConfigIOError(`cannot read config file ${path}: ${errno.message}`,{path:path,errno:errno})}};async function loadConfigFile(path){const text=await fsReader(path);let parsed;try{parsed=JSON.parse(text)}catch(error){throw new ConfigParseError(`malformed JSON in ${path}: ${error.message}`,{path:path})}validateConfig(parsed,{path:path});const resolved=await resolveExtends(parsed,path,fsReader);const out={...resolved};delete out.$schema;return out}const CONFIG_BASENAME=node_path.join(".fsl","config.json");const exists=async path=>{try{await promises.access(path,promises.constants.R_OK);return true}catch{return false}};async function discoverUserGlobalConfig(opts={}){const path=node_path.join(opts.home??node_os.homedir(),CONFIG_BASENAME);if(!await exists(path))return null;return loadConfigFile(path)}async function discoverProjectConfig(opts){let cur=opts.from;const root=node_path.parse(cur).root;for(let i=0;i<64;i++){const candidate=node_path.join(cur,CONFIG_BASENAME);if(await exists(candidate))return loadConfigFile(candidate);if(cur===root)return null;cur=node_path.dirname(cur)}return null}function extractMachineAttributes(_machineSource){return{}}function flagsToConfig(flags,mapping){const out={};for(const[flagName,value]of Object.entries(flags)){const target=mapping[flagName];if(target==null)continue;if(value===void 0)continue;setDotted(out,target,value)}return out}const setDotted=(target,path,value)=>{const parts=path.split(".");let cur=target;for(let i=0;i<parts.length-1;i++){const k=parts[i];if(!Object.prototype.hasOwnProperty.call(cur,k)||typeof cur[k]!=="object"||cur[k]===null)cur[k]={};cur=cur[k]}cur[parts[parts.length-1]]=value};async function loadConfig(opts){const layers=[defaults];if(!opts.skipConfig){if(opts.explicitConfigPath){const explicit=await loadConfigFile(opts.explicitConfigPath);layers.push(explicit)}else{if(!opts.skipUserGlobal){const userGlobal=await discoverUserGlobalConfig({home:opts.home});if(userGlobal)layers.push(userGlobal)}const projectFrom=opts.projectRoot??opts.cwd;const project=await discoverProjectConfig({from:projectFrom});if(project)layers.push(project)}}if(opts.machinePath){try{const source=await promises.readFile(opts.machinePath,"utf8");layers.push(extractMachineAttributes(source))}catch{}}if(opts.flags&&opts.flagMapping){layers.push(flagsToConfig(opts.flags,opts.flagMapping))}return mergeConfigs(layers)}const RENDER_TARGETS=["svg","dot","png","jpeg","html","gif"];async function svgTarget(fsl){try{return await jssmViz.fsl_to_svg_string(fsl)}catch(error){throw new jssmFence.RenderError(`SVG render failed: ${error.message}`)}}function dotTarget(fsl){try{return Promise.resolve(jssmViz.fsl_to_dot(fsl))}catch(error){return Promise.reject(new jssmFence.RenderError(`DOT render failed: ${error.message}`))}}const escapeHtml=s=>s.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;");async function htmlTarget(fsl,opts={}){const title=escapeHtml(opts.title??"FSL Machine");let svg;try{svg=await svgTarget(fsl)}catch(error){throw error instanceof jssmFence.RenderError?error:new jssmFence.RenderError(`HTML render failed: ${String(error)}`)}return`<!DOCTYPE html>\n<html lang="en">\n<head>\n<meta charset="utf-8">\n<title>${title}</title>\n<style>\nbody { font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif; margin: 2rem auto; max-width: 80rem; padding: 0 1rem; color: #222; }\nh1 { font-size: 1.4rem; margin-bottom: 1.5rem; }\n.diagram { display: flex; justify-content: center; }\n.diagram svg { max-width: 100%; height: auto; }\n</style>\n</head>\n<body>\n<h1>${title}</h1>\n<div class="diagram">\n${svg}\n</div>\n</body>\n</html>\n`}async function pngTarget(fsl,opts={}){const svg=await svgTarget(fsl);return jssmFence.rasterize(svg,"png",{width:opts.width,height:opts.height,scale:opts.scale})}async function jpegTarget(fsl,opts={}){const svg=await svgTarget(fsl);return jssmFence.rasterize(svg,"jpeg",{width:opts.width,height:opts.height,scale:opts.scale,quality:opts.quality})}function wrap_gif_error(e){if(e instanceof jssmFence.RasterizationUnsupportedError){throw e}throw new jssmFence.RenderError(`GIF render failed: ${e.message}`)}async function render(fsl,opts){switch(opts.target){case"svg":{return{target:"svg",kind:"text",content:await svgTarget(fsl)}}case"dot":{return{target:"dot",kind:"text",content:await dotTarget(fsl)}}case"html":{return{target:"html",kind:"text",content:await htmlTarget(fsl)}}case"png":{return{target:"png",kind:"raster",buffer:await pngTarget(fsl,{width:opts.width,height:opts.height,scale:opts.scale})}}case"jpeg":{return{target:"jpeg",kind:"raster",buffer:await jpegTarget(fsl,{width:opts.width,height:opts.height,scale:opts.scale,quality:opts.quality})}}case"gif":{try{return{target:"gif",kind:"raster",buffer:await jssmFence.render_fence_gif(fsl,{scale:opts.scale,delay_cs:opts.delay,max_frames:opts.maxFrames})}}catch(error){return wrap_gif_error(error)}}default:{throw new jssmFence.RenderError(`unknown target: ${String(opts.target)}`)}}}const getVersion=()=>"6.0.0-alpha.14";const SPEC={flags:{target:{short:"t",enum:RENDER_TARGETS},output:{short:"o"},"out-dir":{},stdout:{boolean:true},width:{type:"number"},height:{type:"number"},scale:{type:"number"},quality:{type:"number"},delay:{type:"number"},"max-frames":{type:"number"},config:{},"no-config":{boolean:true},help:{short:"h",boolean:true},version:{short:"V",boolean:true}},usage:"fsl-render [options] <fsl-paths...>"};const RENDER_FLAG_TO_CONFIG={target:"render.defaultTarget",output:null,"out-dir":"render.outDir",stdout:null,width:"render.width",height:"render.height",scale:"render.scale",quality:"render.quality",config:null,"no-config":null,help:null,version:null};const extOf=target=>target;const writeStdout=s=>{if(typeof s==="string")process.stdout.write(s);else process.stdout.write(Buffer.from(s))};const writeStderr=s=>{process.stderr.write(s)};const printErr=(msg,path,line)=>{writeStderr(`fsl-render: error: ${msg}\n`);if(path){const loc=line===void 0?`  at ${path}`:`  at ${path} line ${line}`;writeStderr(loc+"\n")}};async function cli(argv){let parsed;try{parsed=parseFslArgs(argv,SPEC)}catch(error){printErr(error.message);return 1}if(parsed.flags.help){writeStdout(parsed.helpText()+"\n");return 0}if(parsed.flags.version){writeStdout(`fsl-render ${getVersion()}\n`);return 0}let config;try{config=await loadConfig({cwd:process.cwd(),machinePath:parsed.positional[0],flags:parsed.flags,flagMapping:RENDER_FLAG_TO_CONFIG,explicitConfigPath:parsed.flags.config,skipConfig:parsed.flags["no-config"]===true})}catch(error){printErr(error.message);return 1}const target=config.render.defaultTarget;const output=parsed.flags.output;const outDir=parsed.flags["out-dir"]??config.render.outDir;const stdout=parsed.flags.stdout===true;const width=config.render.width;const height=config.render.height;const scale=config.render.scale;const quality=config.render.quality;const delay=parsed.flags.delay;const maxFrames=parsed.flags["max-frames"];const outputFlags=[output,outDir,stdout?"--stdout":void 0].filter(x=>x!==void 0);if(outputFlags.length>1){printErr("--output, --out-dir, and --stdout are mutually exclusive");return 1}const sizeFlags=[parsed.flags.width,parsed.flags.height,parsed.flags.scale].filter(x=>x!==void 0);if(sizeFlags.length>1){printErr("--width, --height, and --scale are mutually exclusive");return 1}const MAX_RENDER_DIMENSION=1e5;const validSize=v=>![Number.isFinite(v),v>0,v<=MAX_RENDER_DIMENSION].includes(false);const sizeChecks=[["width",width],["height",height],["scale",scale]];for(const[flagName,value]of sizeChecks){if(value!==void 0&&!validSize(value)){printErr(`--${flagName} must be a positive finite number no greater than ${MAX_RENDER_DIMENSION}`);return 1}}const renderOpts={target:target,width:width,height:height,scale:scale,quality:quality,delay:delay,maxFrames:maxFrames};const inputs=parsed.positional;if(inputs.length===0){if(process.stdin.isTTY){printErr("no input (provide a file or pipe FSL via stdin)");return 1}const fsl=await readStream(process.stdin);return await renderOne(fsl,"<stdin>",renderOpts,{stdout:true})}const explicitStdout=stdout||output==="-";if(inputs.length===1){const path=inputs[0];let fsl;try{fsl=path==="-"?await readStream(process.stdin):await node_fs.promises.readFile(path,"utf8")}catch(error){printErr(`cannot read ${path}: ${error.message}`);return 1}const inputLabel=path==="-"?"<stdin>":path;if(explicitStdout){return renderOne(fsl,inputLabel,renderOpts,{stdout:true})}if(output){return renderOne(fsl,inputLabel,renderOpts,{outputPath:output})}if(outDir){const outPath=node_path.join(outDir,node_path.basename(path,node_path.extname(path))+"."+extOf(target));return renderOne(fsl,inputLabel,renderOpts,{outputPath:outPath})}if(path==="-"){return renderOne(fsl,inputLabel,renderOpts,{stdout:true})}const sibling=node_path.join(node_path.dirname(path),node_path.basename(path,node_path.extname(path))+"."+extOf(target));return renderOne(fsl,inputLabel,renderOpts,{outputPath:sibling})}if(!outDir){printErr("specify --out-dir for multi-file render");return 1}let worstCode=0;const claimedBy=new Map;for(const path of inputs){let fsl;try{fsl=await node_fs.promises.readFile(path,"utf8")}catch(error){printErr(`cannot read ${path}: ${error.message}`);worstCode=Math.max(worstCode,1);continue}const outPath=node_path.join(outDir,node_path.basename(path,node_path.extname(path))+"."+extOf(target));const priorSource=claimedBy.get(outPath);if(priorSource!==void 0){printErr(`refusing to overwrite ${outPath}: both ${priorSource} and ${path} render to it — rename an input or render them in separate --out-dir runs`);worstCode=Math.max(worstCode,1);continue}claimedBy.set(outPath,path);const code=await renderOne(fsl,path,renderOpts,{outputPath:outPath});worstCode=Math.max(worstCode,code)}return worstCode}async function renderOne(fsl,label,opts,out){try{const r=await render(fsl,opts);if("stdout"in out){if(r.kind==="text")writeStdout(r.content);else writeStdout(r.buffer);return 0}const data=r.kind==="text"?r.content:Buffer.from(r.buffer);await node_fs.promises.writeFile(out.outputPath,data);return 0}catch(error){if(error instanceof jssmFence.RenderError){printErr(error.message,label,error.line)}else{printErr(error.message??String(error),label)}return 1}}async function readStream(stream){const chunks=[];for await(const chunk of stream){chunks.push(Buffer.isBuffer(chunk)?chunk:Buffer.from(chunk))}return Buffer.concat(chunks).toString("utf8")}async function main(){const argv=process.argv.slice(2);const code=await cli(argv);process.exitCode=code}void main();