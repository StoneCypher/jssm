import{RenderError,rasterize,render_fence_gif,RasterizationUnsupportedError}from"jssm-fence";export{RasterizationUnsupportedError,RenderError}from"jssm-fence";import{fsl_to_svg_string,fsl_to_dot}from"jssm-viz";import{from}from"jssm";import{readFile,access,constants}from"node:fs/promises";import{homedir}from"node:os";import{join,parse,dirname}from"node:path";import Ajv from"ajv";async function svgTarget(fsl){try{return await fsl_to_svg_string(fsl)}catch(error){throw new RenderError(`SVG render failed: ${error.message}`)}}function dotTarget(fsl){try{return Promise.resolve(fsl_to_dot(fsl))}catch(error){return Promise.reject(new RenderError(`DOT render failed: ${error.message}`))}}const escapeHtml=s=>s.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;");async function htmlTarget(fsl,opts={}){const title=escapeHtml(opts.title??"FSL Machine");let svg;try{svg=await svgTarget(fsl)}catch(error){throw error instanceof RenderError?error:new RenderError(`HTML render failed: ${String(error)}`)}return`<!DOCTYPE html>\n<html lang="en">\n<head>\n<meta charset="utf-8">\n<title>${title}</title>\n<style>\nbody { font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif; margin: 2rem auto; max-width: 80rem; padding: 0 1rem; color: #222; }\nh1 { font-size: 1.4rem; margin-bottom: 1.5rem; }\n.diagram { display: flex; justify-content: center; }\n.diagram svg { max-width: 100%; height: auto; }\n</style>\n</head>\n<body>\n<h1>${title}</h1>\n<div class="diagram">\n${svg}\n</div>\n</body>\n</html>\n`}async function pngTarget(fsl,opts={}){const svg=await svgTarget(fsl);return rasterize(svg,"png",{width:opts.width,height:opts.height,scale:opts.scale})}async function jpegTarget(fsl,opts={}){const svg=await svgTarget(fsl);return rasterize(svg,"jpeg",{width:opts.width,height:opts.height,scale:opts.scale,quality:opts.quality})}function wrap_gif_error(e){if(e instanceof RasterizationUnsupportedError){throw e}throw new RenderError(`GIF render failed: ${e.message}`)}async function render(fsl,opts){switch(opts.target){case"svg":{return{target:"svg",kind:"text",content:await svgTarget(fsl)}}case"dot":{return{target:"dot",kind:"text",content:await dotTarget(fsl)}}case"html":{return{target:"html",kind:"text",content:await htmlTarget(fsl)}}case"png":{return{target:"png",kind:"raster",buffer:await pngTarget(fsl,{width:opts.width,height:opts.height,scale:opts.scale})}}case"jpeg":{return{target:"jpeg",kind:"raster",buffer:await jpegTarget(fsl,{width:opts.width,height:opts.height,scale:opts.scale,quality:opts.quality})}}case"gif":{try{return{target:"gif",kind:"raster",buffer:await render_fence_gif(fsl,{scale:opts.scale,delay_cs:opts.delay,max_frames:opts.maxFrames})}}catch(error){return wrap_gif_error(error)}}default:{throw new RenderError(`unknown target: ${String(opts.target)}`)}}}async function renderSet(inputs,opts){return Promise.all(inputs.map(async(fsl,index)=>{try{const result=await render(fsl,opts);return{ok:true,index:index,result:result}}catch(error){return{ok:false,index:index,error:error}}}))}class CodegenError extends Error{constructor(message,opts={}){super(message);this.name="CodegenError";this.path=opts.path;this.line=opts.line;this.column=opts.column;Object.setPrototypeOf(this,CodegenError.prototype)}}class CodegenUndecidedError extends CodegenError{constructor(message,opts={}){super(message,opts);this.name="CodegenUndecidedError";Object.setPrototypeOf(this,CodegenUndecidedError.prototype)}}function extractSurface(fsl){let machine;try{machine=from(fsl)}catch(error){throw new CodegenError(`codegen failed to compile FSL: ${error.message}`)}const states=machine.states().map(String);const initial=String(machine.state());const actions=machine.list_actions().map(String);const finals=states.filter(s=>machine.state_is_final(s));const transitions=[];const eventless=[];for(const edge of machine.list_edges()){if(edge.action===void 0){eventless.push({from:String(edge.from),to:String(edge.to)});continue}transitions.push({from:String(edge.from),action:String(edge.action),to:String(edge.to)})}return{states:states,initial:initial,actions:actions,finals:finals,transitions:transitions,eventless:eventless}}function jsStringLiteralBody(raw){return raw.replace(/\\/g,"\\\\").replace(/'/g,String.raw`\'`).replace(/\n/g,String.raw`\n`).replace(/\r/g,String.raw`\r`).replace(/\t/g,String.raw`\t`).replace(/\f/g,String.raw`\f`).replace(/\v/g,String.raw`\v`).replace(/\u{8}/gu,String.raw`\b`).replace(/\0/g,String.raw`\x00`)}function toSymbol(label,fallback){const words=label.split(/[^A-Z0-9]+/i).filter(w=>w.length>0);if(words.length===0)return fallback;const pascal=words.map(w=>w.charAt(0).toUpperCase()+w.slice(1)).join("");return/^\d/.test(pascal)?`M${pascal}`:pascal}function emitUnion(members){if(members.length===0)return"never";return members.map(m=>`'${jsStringLiteralBody(m)}'`).join(" | ")}function emitTransitionTable$1(surface,indent){const byFrom=new Map;for(const s of surface.states)byFrom.set(s,[]);for(const t of surface.transitions){byFrom.get(t.from).push({action:t.action,to:t.to})}const stateLines=surface.states.map(state=>{const edges=byFrom.get(state);const edgeBody=edges.map(e=>`${indent}    '${jsStringLiteralBody(e.action)}': '${jsStringLiteralBody(e.to)}'`).join(",\n");const inner=edges.length===0?"{}":`{\n${edgeBody}\n${indent}  }`;return`${indent}  '${jsStringLiteralBody(state)}': ${inner}`});return`{\n${stateLines.join(",\n")}\n${indent}}`}function emitNativeTypescript(surface,symbol){const table=emitTransitionTable$1(surface,"  ");const eventless="{"+surface.eventless.map(e=>`'${jsStringLiteralBody(e.from)}': '${jsStringLiteralBody(e.to)}'`).join(", ")+"}";const stateType=`${symbol}State`;const actionType=`${symbol}Action`;return`// Generated by \`fsl codegen --target native:typescript\`. Do not edit by hand.\n// A self-contained, dependency-free implementation of the source machine.\n\nexport type ${stateType} = ${emitUnion(surface.states)};\nexport type ${actionType} = ${emitUnion(surface.actions)};\n\nexport class ${symbol} {\n  static readonly states: readonly ${stateType}[] = [${surface.states.map(s=>`'${jsStringLiteralBody(s)}'`).join(", ")}];\n  static readonly actions: readonly ${actionType}[] = [${surface.actions.map(a=>`'${jsStringLiteralBody(a)}'`).join(", ")}];\n  static readonly finals: readonly ${stateType}[] = [${surface.finals.map(s=>`'${jsStringLiteralBody(s)}'`).join(", ")}];\n\n  private static readonly transitions: Readonly<Record<${stateType}, Partial<Record<${actionType}, ${stateType}>>>> = ${table};\n  private static readonly eventless: Partial<Record<${stateType}, ${stateType}>> = ${eventless};\n\n  private current: ${stateType} = '${jsStringLiteralBody(surface.initial)}';\n\n  /** The current state. */\n  get state(): ${stateType} {\n    return this.current;\n  }\n\n  /** The action names fireable from the current state. */\n  actions(): ${actionType}[] {\n    return Object.keys(${symbol}.transitions[this.current]) as ${actionType}[];\n  }\n\n  /** Whether \`name\` can fire from the current state. */\n  can(name: ${actionType}): boolean {\n    return Object.prototype.hasOwnProperty.call(${symbol}.transitions[this.current], name);\n  }\n\n  /** Fire \`name\`; advances state and returns true, or returns false if not allowed. */\n  action(name: ${actionType}): boolean {\n    const next = ${symbol}.transitions[this.current][name];\n    if (next === undefined) return false;\n    this.current = next;\n    return true;\n  }\n\n  /** Whether the current state is a final (terminal or complete) state. */\n  isFinal(): boolean {\n    return (${symbol}.finals as readonly ${stateType}[]).includes(this.current);\n  }\n\n  /** Take the eventless/automatic transition from the current state, if any. */\n  step(): boolean {\n    const next = ${symbol}.eventless[this.current];\n    if (next === undefined) return false;\n    this.current = next;\n    return true;\n  }\n}\n`}function emitTransitionTable(surface,indent){const byFrom=new Map;for(const s of surface.states)byFrom.set(s,[]);for(const t of surface.transitions){byFrom.get(t.from).push({action:t.action,to:t.to})}const stateLines=surface.states.map(state=>{const edges=byFrom.get(state);const edgeBody=edges.map(e=>`${indent}    '${jsStringLiteralBody(e.action)}': '${jsStringLiteralBody(e.to)}'`).join(",\n");const inner=edges.length===0?"{}":`{\n${edgeBody}\n${indent}  }`;return`${indent}  '${jsStringLiteralBody(state)}': ${inner}`});return`{\n${stateLines.join(",\n")}\n${indent}}`}function emitNativeJavascript(surface,symbol){const table=emitTransitionTable(surface,"  ");const eventless="{"+surface.eventless.map(e=>`'${jsStringLiteralBody(e.from)}': '${jsStringLiteralBody(e.to)}'`).join(", ")+"}";return`// Generated by \`fsl codegen --target native:javascript\`. Do not edit by hand.\n// A self-contained, dependency-free implementation of the source machine.\n\nexport class ${symbol} {\n  static states = [${surface.states.map(s=>`'${jsStringLiteralBody(s)}'`).join(", ")}];\n  static actions = [${surface.actions.map(a=>`'${jsStringLiteralBody(a)}'`).join(", ")}];\n  static finals = [${surface.finals.map(s=>`'${jsStringLiteralBody(s)}'`).join(", ")}];\n\n  static #transitions = ${table};\n  static #eventless = ${eventless};\n\n  #state = '${jsStringLiteralBody(surface.initial)}';\n\n  /** The current state. */\n  get state() {\n    return this.#state;\n  }\n\n  /** The action names fireable from the current state. */\n  actions() {\n    return Object.keys(${symbol}.#transitions[this.#state] ?? {});\n  }\n\n  /** Whether \`name\` can fire from the current state. */\n  can(name) {\n    return Object.prototype.hasOwnProperty.call(${symbol}.#transitions[this.#state] ?? {}, name);\n  }\n\n  /** Fire \`name\`; advances state and returns true, or returns false if not allowed. */\n  action(name) {\n    const next = (${symbol}.#transitions[this.#state] ?? {})[name];\n    if (next === undefined) return false;\n    this.#state = next;\n    return true;\n  }\n\n  /** Whether the current state is a final (terminal or complete) state. */\n  isFinal() {\n    return ${symbol}.finals.includes(this.#state);\n  }\n\n  /** Take the eventless/automatic transition from the current state, if any. */\n  step() {\n    const next = ${symbol}.#eventless[this.#state];\n    if (next === undefined) return false;\n    this.#state = next;\n    return true;\n  }\n}\n`}const TARGET_META={"native:typescript":{host:"typescript",extension:"ts"},"native:javascript":{host:"javascript",extension:"js"}};function assertWithinBudget(elapsedMs,budgetMs){if(budgetMs!==void 0&&budgetMs>0&&elapsedMs>budgetMs){throw new CodegenUndecidedError(`codegen exceeded budget of ${budgetMs}ms`)}}function codegen(fsl,opts){const meta=TARGET_META[opts.target];if(meta===void 0){throw new CodegenError(`unknown target: ${opts.target}`)}if(opts.certify){throw new CodegenUndecidedError(`--certify is not available for ${opts.target} in this build: the conformance harness is gated on the certified runtime (megaspec §26)`)}const clock=opts.now??Date.now;const startedAt=clock();const surface=extractSurface(fsl);assertWithinBudget(clock()-startedAt,opts.budgetMs);const symbol=toSymbol(opts.name??"","Machine");const content=opts.target==="native:typescript"?emitNativeTypescript(surface,symbol):emitNativeJavascript(surface,symbol);return{target:opts.target,host:meta.host,extension:meta.extension,content:content,symbol:symbol}}function codegenSet(inputs,opts){return inputs.map((fsl,index)=>{try{return{ok:true,index:index,artifact:codegen(fsl,opts)}}catch(error){return{ok:false,index:index,error:error}}})}const EXPORT_IMPLEMENTED=new Set(["json","mermaid"]);const IMPORT_IMPLEMENTED=new Set(["json","mermaid"]);class InterchangeError extends Error{constructor(message,opts){super(message);this.name="InterchangeError";this.reason=opts.reason;this.format=opts.format;Object.setPrototypeOf(this,InterchangeError.prototype)}}const kindToInterchange=kind=>kind==="main"||kind==="forced"?kind:"legal";const ARROW={legal:"->",main:"=>",forced:"~>"};function quoteState(name){const escaped=name.replace(/\\/g,"\\\\").replace(/"/g,String.raw`\"`);return`"${escaped}"`}function quoteAction(action){const escaped=action.replace(/\\/g,"\\\\").replace(/'/g,String.raw`\'`);return`'${escaped}'`}function fslToModel(fsl){let machine;try{machine=from(fsl)}catch(error){throw new InterchangeError(`cannot parse FSL: ${error.message??String(error)}`,{reason:"parse",format:"fsl"})}const states=machine.states();const edges=machine.list_edges().map(tr=>{const edge={from:tr.from,to:tr.to,kind:kindToInterchange(tr.kind)};if(tr.action!==void 0&&tr.action!==null){edge.action=tr.action}return edge});return{states:states,edges:edges,start_states:[machine.state()]}}function modelToFsl(model){const lines=[];const connected=new Set;for(const edge of model.edges){connected.add(edge.from);connected.add(edge.to);const action=edge.action===void 0?"":` ${quoteAction(edge.action)}`;lines.push(`${quoteState(edge.from)}${action} ${ARROW[edge.kind]} ${quoteState(edge.to)};`)}const isolatedStates=[];for(const state of model.states){if(connected.has(state)){continue}isolatedStates.push(state);lines.push(`${quoteState(state)} ${ARROW.legal} ${quoteState(state)};`)}return{fsl:lines.join("\n")+(lines.length>0?"\n":""),isolatedStates:isolatedStates}}function modelToJson(model){const doc={format:"fsl-interchange",version:1,states:model.states,edges:model.edges};if(model.start_states!==void 0){doc.start_states=model.start_states}return JSON.stringify(doc,null,2)+"\n"}const VALID_KINDS=new Set(["legal","main","forced"]);function jsonToModel(text){let raw;try{raw=JSON.parse(text)}catch(error){throw new InterchangeError(`invalid JSON: ${error.message}`,{reason:"parse",format:"json"})}if(typeof raw!=="object"||raw===null){throw new InterchangeError("JSON document must be an object",{reason:"parse",format:"json"})}const doc=raw;if(!Array.isArray(doc.states)||doc.states.some(s=>typeof s!=="string")){throw new InterchangeError("JSON `states` must be an array of strings",{reason:"parse",format:"json"})}if(!Array.isArray(doc.edges)){throw new InterchangeError("JSON `edges` must be an array",{reason:"parse",format:"json"})}const edges=doc.edges.map((e,i)=>{if(typeof e!=="object"||e===null){throw new InterchangeError(`JSON edge ${i} must be an object`,{reason:"parse",format:"json"})}const edge=e;if(typeof edge.from!=="string"||typeof edge.to!=="string"){throw new InterchangeError(`JSON edge ${i} requires string \`from\` and \`to\``,{reason:"parse",format:"json"})}if(typeof edge.kind!=="string"||!VALID_KINDS.has(edge.kind)){throw new InterchangeError(`JSON edge ${i} \`kind\` must be one of legal, main, forced`,{reason:"parse",format:"json"})}const out={from:edge.from,to:edge.to,kind:edge.kind};if(edge.action!==void 0){if(typeof edge.action!=="string"){throw new InterchangeError(`JSON edge ${i} \`action\` must be a string`,{reason:"parse",format:"json"})}out.action=edge.action}return out});const model={states:doc.states,edges:edges};if(Array.isArray(doc.start_states)&&doc.start_states.every(s=>typeof s==="string")){model.start_states=doc.start_states}return model}const escapeLabel=name=>name.replace(/"/g,"#quot;");const buildIdMap=states=>{const map=new Map;for(const[i,s]of states.entries())map.set(s,`s${i}`);return map};function modelToMermaid(model){const ids=buildIdMap(model.states);const lines=["stateDiagram-v2"];for(const state of model.states){lines.push(`  ${ids.get(state)}: ${escapeLabel(state)}`)}for(const edge of model.edges){const fromId=ids.get(edge.from)??edge.from;const toId=ids.get(edge.to)??edge.to;const label=edge.action===void 0?"":` : ${escapeLabel(edge.action)}`;lines.push(`  ${fromId} --\x3e ${toId}${label}`)}return lines.join("\n")+"\n"}const unescapeLabel=label=>label.replace(/#quot;/g,'"');const TRANSITION_RE=/^(\w+)\s*-->\s*(\w+)(?:\s*:\s*(\S.*)?)?$/;const NODE_RE=/^(\w+)\s*:\s*(\S.*)?$/;function mermaidToModel(text){const rawLines=text.split(/\r?\n/);const labelOf=new Map;const order=[];const edges=[];let sawHeader=false;const note=id=>{if(labelOf.has(id)){return}labelOf.set(id,id);order.push(id)};for(const rawLine of rawLines){const line=rawLine.trim();if(line==="")continue;if(line==="stateDiagram-v2"||line==="stateDiagram"){sawHeader=true;continue}const tr=TRANSITION_RE.exec(line);if(tr){const[,from,to,label]=tr;note(from);note(to);const edge={from:from,to:to,kind:"legal"};if(label!==void 0&&label.trim()!==""){edge.action=unescapeLabel(label.trim())}edges.push(edge);continue}const nd=NODE_RE.exec(line);if(nd){const[,id,label]=nd;note(id);labelOf.set(id,unescapeLabel((label??"").trim()));continue}throw new InterchangeError(`unrecognized mermaid line: ${line}`,{reason:"parse",format:"mermaid"})}if(!sawHeader){throw new InterchangeError("not a mermaid state diagram (missing `stateDiagram-v2` header)",{reason:"parse",format:"mermaid"})}const nameOf=id=>labelOf.get(id);return{states:order.map(nameOf),edges:edges.map(e=>({...e,from:nameOf(e.from),to:nameOf(e.to)}))}}const KNOWN_FORMATS$1=new Set(["json","mermaid","scxml","xstate","dot","tla+","alloy","smv","promela","gbnf","lark","ebnf"]);function exportMachine(fsl,opts){const{format:format}=opts;if(!KNOWN_FORMATS$1.has(format)){throw new InterchangeError(`unknown export format: ${format}`,{reason:"unknown-format",format:format})}if(!EXPORT_IMPLEMENTED.has(format)){throw new InterchangeError(`export to '${format}' is a documented opt-in not yet implemented (megaspec §25)`,{reason:"unsupported",format:format})}const model=fslToModel(fsl);if(format==="json"){return{output:modelToJson(model),lossy:[]}}const lossy=[];if(model.edges.some(e=>e.kind!=="legal")){lossy.push("mermaid has no arrow-kind distinction; 'main' and 'forced' edges export as plain transitions")}return{output:modelToMermaid(model),lossy:lossy}}const KNOWN_FORMATS=new Set(["json","mermaid","scxml","xstate","dot"]);function importMachine(source,opts){const{format:format}=opts;if(!KNOWN_FORMATS.has(format)){throw new InterchangeError(`unknown import format: ${format}`,{reason:"unknown-format",format:format})}if(!IMPORT_IMPLEMENTED.has(format)){throw new InterchangeError(`import from '${format}' is a documented opt-in not yet implemented (megaspec §25)`,{reason:"unsupported",format:format})}let model;const lossy=[];if(format==="json"){model=jsonToModel(source)}else{model=mermaidToModel(source);if(model.edges.length>0){lossy.push("mermaid carries no arrow kind; all transitions imported as legal '->' edges")}}const{fsl:fsl,isolatedStates:isolatedStates}=modelToFsl(model);if(isolatedStates.length>0){lossy.push(`FSL cannot declare edgeless states; materialized as self-loops: ${isolatedStates.join(", ")}`)}return{output:fsl,lossy:lossy}}function parseFslArgs(argv,spec){const positional=[];const flags={};const shortMap={};for(const[name,fs]of Object.entries(spec.flags)){if(fs.short)shortMap[fs.short]=name}const isBoolean=name=>spec.flags[name]?.boolean;const flagType=name=>{const fs=spec.flags[name];return fs.type??"string"};const coerce=(name,raw)=>{const t=flagType(name);if(t==="number"){const n=Number(raw);if(Number.isNaN(n)){throw new TypeError(`flag --${name} requires a number, got: ${raw}`)}return n}const fs=spec.flags[name];if(fs.enum&&!fs.enum.includes(raw)){throw new Error(`flag --${name} value '${raw}' not in: ${fs.enum.join(", ")}`)}return raw};const missingValue=idx=>idx>=argv.length||argv[idx].startsWith("--");let i=0;let positionalOnly=false;while(i<argv.length){const a=argv[i];if(positionalOnly){positional.push(a);i++;continue}if(a==="--"){positionalOnly=true;i++;continue}if(a==="-"){positional.push(a);i++;continue}if(a.startsWith("--")){const eq=a.indexOf("=");const name=eq===-1?a.slice(2):a.slice(2,eq);if(!Object.prototype.hasOwnProperty.call(spec.flags,name))throw new Error(`unknown flag: --${name}`);if(isBoolean(name)){flags[name]=true;i++}else if(eq===-1){if(missingValue(i+1))throw new Error(`flag --${name} requires a value`);flags[name]=coerce(name,argv[i+1]);i+=2}else{flags[name]=coerce(name,a.slice(eq+1));i++}continue}if(a.startsWith("-")&&a.length>1){const short=a[1];const name=shortMap[short];if(!name)throw new Error(`unknown flag: -${short}`);if(isBoolean(name)){flags[name]=true;if(a.length>2)throw new Error(`combined short flags not supported: ${a}`);i++}else if(a.length>2){flags[name]=coerce(name,a.slice(2));i++}else{if(missingValue(i+1))throw new Error(`flag -${short} requires a value`);flags[name]=coerce(name,argv[i+1]);i+=2}continue}positional.push(a);i++}for(const[name,fs]of Object.entries(spec.flags)){if(flags[name]===void 0&&fs.default!==void 0){flags[name]=fs.default}}const helpText=()=>{const lines=["Usage:","  "+spec.usage,"","Options:"];for(const[name,fs]of Object.entries(spec.flags)){const short=fs.short?`-${fs.short}, `:" ".repeat(4);const longPart=`--${name}`;let arg=" VALUE";if(fs.boolean){arg=""}else if(fs.enum){arg=` ${fs.enum.join("|")}`}else if(fs.type==="number"){arg=" N"}const defStr=fs.default===void 0?"":` (default: ${fs.default})`;lines.push(`  ${short}${longPart}${arg}${defStr}`)}return lines.join("\n")};return{positional:positional,flags:flags,helpText:helpText}}const isPlainObject=v=>typeof v==="object"&&v!==null&&!Array.isArray(v)&&Object.getPrototypeOf(v)===Object.prototype;const mergeTwo=(a,b)=>{if(b===void 0)return a;if(b===null)return null;if(isPlainObject(a)&&isPlainObject(b)){const out={...a};for(const[k,value]of Object.entries(b)){const merged=mergeTwo(a[k],value);if(merged===void 0){delete out[k]}else{out[k]=merged}}return out}return b};function mergeConfigs(layers){let acc={};for(const layer of layers){acc=mergeTwo(acc,layer)}return acc}const deepFreeze=o=>{if(o&&typeof o==="object"){for(const value of Object.values(o)){deepFreeze(value)}Object.freeze(o)}return o};const defaults=deepFreeze({include:["**/*.fsl"],exclude:["**/node_modules/**"],render:{defaultTarget:"svg",scale:3,quality:85},lint:{},format:{},test:{},check:{},typegen:{},codegen:{},init:{},import:{},export:{},mcp:{},lsp:{},repl:{},registry:{}});class ConfigError extends Error{constructor(message,opts={}){super(message);this.name=this.constructor.name;this.path=opts.path;this.line=opts.line;this.column=opts.column}}class ConfigParseError extends ConfigError{constructor(message,opts={}){super(message,opts);this.kind="parse";Object.setPrototypeOf(this,ConfigParseError.prototype)}}class ConfigSchemaError extends ConfigError{constructor(message,opts){super(message,opts);this.kind="schema";this.violations=opts.violations;Object.setPrototypeOf(this,ConfigSchemaError.prototype)}}class ConfigExtendsError extends ConfigError{constructor(message,opts){super(message,{path:opts.path});this.kind="extends";this.chain=opts.chain;Object.setPrototypeOf(this,ConfigExtendsError.prototype)}}class ConfigIOError extends ConfigError{constructor(message,opts){super(message,{path:opts.path});this.kind="io";this.errno=opts.errno;Object.setPrototypeOf(this,ConfigIOError.prototype)}}const emptySection={type:"object",additionalProperties:true};const CONFIG_SCHEMA={$schema:"http://json-schema.org/draft-07/schema#",$id:"https://stonecypher.github.io/jssm/schemas/fsl-config.json",title:"fsl Configuration",description:"Unified configuration for the fsl CLI and the jssm/cli library.",type:"object",additionalProperties:false,properties:{$schema:{type:"string"},extends:{oneOf:[{type:"string"},{type:"array",items:{type:"string"}}]},include:{type:"array",items:{type:"string"}},exclude:{type:"array",items:{type:"string"}},render:{type:"object",additionalProperties:false,properties:{defaultTarget:{type:"string",enum:["svg","dot","png","jpeg","html"]},outDir:{type:"string"},scale:{type:"number"},width:{type:"number"},height:{type:"number"},quality:{type:"number"},theme:{type:"string"}}},lint:emptySection,format:emptySection,test:emptySection,check:emptySection,typegen:emptySection,codegen:emptySection,init:emptySection,import:emptySection,export:emptySection,mcp:emptySection,lsp:emptySection,repl:emptySection,registry:{type:"object",additionalProperties:{type:"string"}}}};const ajv=new Ajv({allErrors:true});const validator=ajv.compile(CONFIG_SCHEMA);function validateConfig(config,opts={}){const ok=validator(config);if(!ok){const violations=validator.errors;const summary=violations.map(v=>`${v.instancePath||"/"}: ${v.message}`).join("; ");throw new ConfigSchemaError(`config schema violation${opts.path?` in ${opts.path}`:""}: ${summary}`,{path:opts.path,violations:violations})}}const MAX_DEPTH=32;const dirnameOf=path=>{const ix=Math.max(path.lastIndexOf("/"),path.lastIndexOf("\\"));return ix===-1?".":path.slice(0,ix)};const joinPath=(dir,rel)=>{if(/^(?:[a-z]:)?[/\\]/i.test(rel))return rel;const combined=dir+"/"+rel;const parts=combined.split(/[/\\]+/);const out=[];for(const p of parts){if(p==="."||p==="")continue;if(p==="..")out.pop();else out.push(p)}const prefix=/^[a-z]:/i.test(combined)?"":"/";return prefix+out.join("/")};const omitKey=(obj,key)=>{const out={};for(const[k,value]of Object.entries(obj))if(k!==key)out[k]=value;return out};async function resolveExtends(raw,basePath,reader,visited=[]){if(visited.includes(basePath)){throw new ConfigExtendsError(`extends cycle detected: ${[...visited,basePath].join(" -> ")}`,{path:basePath,chain:[...visited,basePath]})}if(visited.length>=MAX_DEPTH){throw new ConfigExtendsError(`extends depth ${MAX_DEPTH} exceeded`,{path:basePath,chain:[...visited,basePath]})}if(!raw.extends){return omitKey(raw,"extends")}const extendsList=typeof raw.extends==="string"?[raw.extends]:raw.extends;const baseDir=dirnameOf(basePath);const nextVisited=[...visited,basePath];const bases=[];for(const rel of extendsList){const absPath=joinPath(baseDir,rel);const text=await reader(absPath);let parsed;try{parsed=JSON.parse(text)}catch(error){throw new ConfigParseError(`malformed JSON in ${absPath}: ${error.message}`,{path:absPath})}validateConfig(parsed,{path:absPath});const resolved=await resolveExtends(parsed,absPath,reader,nextVisited);bases.push(resolved)}const ownKeys=omitKey(raw,"extends");return mergeConfigs([...bases,ownKeys])}const fsReader=async path=>{try{return await readFile(path,"utf8")}catch(error){const errno=error;throw new ConfigIOError(`cannot read config file ${path}: ${errno.message}`,{path:path,errno:errno})}};async function loadConfigFile(path){const text=await fsReader(path);let parsed;try{parsed=JSON.parse(text)}catch(error){throw new ConfigParseError(`malformed JSON in ${path}: ${error.message}`,{path:path})}validateConfig(parsed,{path:path});const resolved=await resolveExtends(parsed,path,fsReader);const out={...resolved};delete out.$schema;return out}const CONFIG_BASENAME=join(".fsl","config.json");const exists=async path=>{try{await access(path,constants.R_OK);return true}catch{return false}};async function discoverUserGlobalConfig(opts={}){const path=join(opts.home??homedir(),CONFIG_BASENAME);if(!await exists(path))return null;return loadConfigFile(path)}async function discoverProjectConfig(opts){let cur=opts.from;const root=parse(cur).root;for(let i=0;i<64;i++){const candidate=join(cur,CONFIG_BASENAME);if(await exists(candidate))return loadConfigFile(candidate);if(cur===root)return null;cur=dirname(cur)}return null}function extractMachineAttributes(_machineSource){return{}}function flagsToConfig(flags,mapping){const out={};for(const[flagName,value]of Object.entries(flags)){const target=mapping[flagName];if(target==null)continue;if(value===void 0)continue;setDotted(out,target,value)}return out}const setDotted=(target,path,value)=>{const parts=path.split(".");let cur=target;for(let i=0;i<parts.length-1;i++){const k=parts[i];if(!Object.prototype.hasOwnProperty.call(cur,k)||typeof cur[k]!=="object"||cur[k]===null)cur[k]={};cur=cur[k]}cur[parts[parts.length-1]]=value};async function loadConfig(opts){const layers=[defaults];if(!opts.skipConfig){if(opts.explicitConfigPath){const explicit=await loadConfigFile(opts.explicitConfigPath);layers.push(explicit)}else{if(!opts.skipUserGlobal){const userGlobal=await discoverUserGlobalConfig({home:opts.home});if(userGlobal)layers.push(userGlobal)}const projectFrom=opts.projectRoot??opts.cwd;const project=await discoverProjectConfig({from:projectFrom});if(project)layers.push(project)}}if(opts.machinePath){try{const source=await readFile(opts.machinePath,"utf8");layers.push(extractMachineAttributes(source))}catch{}}if(opts.flags&&opts.flagMapping){layers.push(flagsToConfig(opts.flags,opts.flagMapping))}return mergeConfigs(layers)}export{CONFIG_SCHEMA,CodegenError,CodegenUndecidedError,ConfigError,ConfigExtendsError,ConfigIOError,ConfigParseError,ConfigSchemaError,InterchangeError,codegen,codegenSet,defaults,discoverProjectConfig,discoverUserGlobalConfig,exportMachine,extractMachineAttributes,extractSurface,flagsToConfig,importMachine,loadConfig,loadConfigFile,mergeConfigs,parseFslArgs,render,renderSet,resolveExtends,validateConfig};